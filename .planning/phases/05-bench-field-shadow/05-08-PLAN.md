---
phase: 05-bench-field-shadow
plan: 08
type: execute
wave: 6
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05, 05-06, 05-07]
files_modified:
  - tests/fixtures/replay/v1-30d/
  - tests/fixtures/fleet/
  - .planning/phases/05-bench-field-shadow/SIGNOFF.md
  - .planning/phases/05-bench-field-shadow/replay-summary-phase5-exit.json
  - artifacts/soak-zao-violations.json
  - artifacts/soak-exhausted-violations.json
autonomous: false
requirements:
  - R-01
  - R-02
  - R-04
  - S-02
  - S-03
  - S-04
  - X-04
  - M1
  - M6
  - F-04
tags:
  - operator-manual
  - soak
  - phase-5
  - exit-gate
must_haves:
  truths:
    - "R-01: tests/fixtures/replay/v1-30d/ updated via a single LFS PR containing >= 30 days of redacted v1 traces, merged BEFORE bench soak begins"
    - "Bench Jetson 1-week clean soak completed: 0 daemon crashes, 0 act-on-Zao-active, 0 unexplained Exhausted (F-04 budget: 1 minor/week)"
    - "S-03 handoff gate passed: bench-week S-01 gates green before field deploy begins"
    - "Field box 2-week clean soak completed: same gates"
    - "X-04: all fleet boxes' triple.json captured under tests/fixtures/fleet/<box-id>/ via the X-04 batched Phase-6-prereq PR (committed AND merged)"
    - "R-02: pytest tests/replay/test_v1_agreement.py against freshly-pulled v1-30d bundle achieves >= 0.95 fault-cycle agreement; replay-summary-phase5-exit.json committed"
    - "SIGNOFF.md filled in by on-site engineer and committed at Phase 5 exit"
  artifacts:
    - path: "tests/fixtures/replay/v1-30d/"
      provides: "Day-1 fresh v1 trace bundle (LFS); replaces or augments the Plan 04-06 placeholder"
    - path: "tests/fixtures/fleet/<box-id>/triple.json"
      provides: "Per-box fleet fixture; N files committed via X-04 batched PR"
    - path: ".planning/phases/05-bench-field-shadow/SIGNOFF.md"
      provides: "Filled signoff with engineer attestation + replay-harness rate + violations log"
    - path: ".planning/phases/05-bench-field-shadow/replay-summary-phase5-exit.json"
      provides: "Machine-readable R-02 result, committed alongside SIGNOFF.md"
  key_links:
    - from: "bench/field soak observation"
      to: "SIGNOFF.md F-04 violations log + S-01 gates table"
      via: "operator-filled markdown"
      pattern: "Phase 5 Sign-off"
    - from: "X-04 fleet-fixture batched PR merge"
      to: "/etc/spark-modem-watchdog/known-fleet/<box-id>/triple.json after .deb rebuild"
      via: "Plan 06 debian/install line"
      pattern: "known-fleet"
---

<objective>
This is the **operator-facing manual plan** that executes Phase 5 in the real world.
The plan covers the time-bound, hardware-dependent, judgment-bearing steps the on-site
engineer performs over ~3+ weeks: the R-01 day-1 trace pull, the 1-week bench soak,
the S-03 handoff gate, the 2-week field soak, the X-04 fleet-fixture capture sweep,
the R-02 replay-harness one-shot, and the final SIGNOFF.md authoring + commit.

Purpose: Phase 5's exit gate is human-attested. No automated script can declare
"bench Jetson ran clean for 1 week with no daemon crashes" — only the engineer
watching it can. This plan structures their work as a sequenced operator checklist
with explicit success criteria per stage.

Output: Six committed artifacts (LFS PR for v1-30d, X-04 batched fleet-fixture PR,
filled SIGNOFF.md, replay-summary-phase5-exit.json, two audit JSONs). NO code, NO
tests. All steps reference Plan 07's SOAK_RUNBOOK.md as the authoritative procedure;
this plan is the SEQUENCING + ACCEPTANCE OVERLAY.

Schedule: this plan spans ~3-4 weeks calendar time. The executor (Claude) does NOT
run automated commands — every task is a checklist for the human operator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-bench-field-shadow/05-CONTEXT.md
@.planning/phases/05-bench-field-shadow/05-RESEARCH.md
@.planning/phases/05-bench-field-shadow/SOAK_RUNBOOK.md
@.planning/phases/05-bench-field-shadow/SIGNOFF.md

<interfaces>
This plan is a checklist driver. The interfaces are the OUTPUTS of earlier plans:

- `tools/audit_soak_zao.py` (Plan 05) — exit 0 = clean
- `tools/audit_soak_exhausted.py` (Plan 05) — exit 0 = clean
- `tools/pull_replay_traces.py` (Plan 04-06, pre-existing) — R-01 LFS pull
- `pytest tests/replay/test_v1_agreement.py` (Plan 02-10 + 04-07, pre-existing) — R-02 gate ≥ 0.95
- `spark-modem ctl capture-fleet-fixture --out=<dir>` (Plan 03) — X-04 sweep
- `.planning/phases/05-bench-field-shadow/SIGNOFF.md` (Plan 07) — exit signoff template
- `.planning/phases/05-bench-field-shadow/SOAK_RUNBOOK.md` (Plan 07) — daily-check + soak-exit procedure
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 (R-01): Day-1 v1-trace pull + LFS PR merge</name>
  <files>
    tests/fixtures/replay/v1-30d/
  </files>
  <read_first>
    - .planning/phases/05-bench-field-shadow/SOAK_RUNBOOK.md § 5 (R-01 day-1 trace pull procedure)
    - tools/pull_replay_traces.py (tool docstring + --help)
    - tests/fixtures/replay/v1-30d/README.md (redaction contract — sha256[:8] for ICCID/IMSI/IP)
  </read_first>
  <rationale>
    R-04 anchor: the quarterly v1-trace refresh cadence (deferred from Phase 4)
    begins with this day-1 pull per CONTEXT.md R-04. Subsequent quarterly
    refreshes are deferred to Phase 6/7 ownership; Phase 5 only owns the first
    refresh.
  </rationale>
  <what-built>
    Nothing automated. This task is a checklist for the on-site engineer to execute
    BEFORE bench soak begins. The plan structures the procedure that was already
    designed in Plan 04-06.
  </what-built>
  <how-to-verify>
    Operator checklist (numbered for sequencing):

    1. Archive `/var/log/spark-modem-watchdog/` from EACH (now-decommissioned) v1 box.
       One tarball per box. Preserve filenames; the redaction script reads relative
       paths within the archive.

    2. On the dev laptop, ensure `git-lfs` is installed and authenticated against
       the repo's LFS remote (`git lfs install`, `git lfs pull`).

    3. Run the redaction + ingestion tool:
       ```
       python tools/pull_replay_traces.py \
         --input-archive <path-to-archive-bundle> \
         --output-dir tests/fixtures/replay/v1-30d/
       ```
       (Adapt argv to the tool's actual flags — read `python tools/pull_replay_traces.py --help`.)

    4. Verify redaction contract: `grep -rE '[0-9]{18,22}' tests/fixtures/replay/v1-30d/ | head`
       should produce ZERO matches outside of the `<redacted:[0-9a-f]{8}>` form.
       If raw long-digit runs remain → STOP, the redaction missed; do NOT commit.

    5. Verify ≥30 days of coverage:
       ```
       find tests/fixtures/replay/v1-30d/ -name '*.jsonl' | wc -l
       ```
       OR (depending on bundle shape) inspect timestamp range across files.
       Document the actual coverage in the PR description.

    6. Stage + commit + open LFS PR:
       ```
       git add tests/fixtures/replay/v1-30d/
       git lfs status   # confirms LFS-tracked files
       git commit -m "phase 5: R-01 day-1 v1-trace refresh (Phase 5 kickoff)"
       git push --set-upstream origin <branch>
       gh pr create --title "phase 5: R-01 day-1 v1-trace refresh" --body "..."
       ```

    7. **Wait for PR merge.** Bench soak CANNOT begin until the merge lands; the
       replay-harness at Phase 5 exit (R-02, Task 6) runs against this exact bundle.
  </how-to-verify>
  <resume-signal>
    Type "approved: R-01 PR merged at <short-sha>" once the LFS PR is merged.
    Provide the merge-commit short SHA for the audit trail. Resume to Task 2.
  </resume-signal>
  <acceptance_criteria>
    - `git log --oneline tests/fixtures/replay/v1-30d/ | head -1 | grep -i "phase.5.*R-01\|R-01.*phase.5"` returns 1 match (commit message wired)
    - `grep -rE '[0-9]{18,22}' tests/fixtures/replay/v1-30d/ | grep -v '<redacted:' | head` returns ZERO matches (no raw PII survives)
    - The merge-commit short SHA is recorded in the resume-signal response (later transcribed into SIGNOFF.md "Bundle source" field at Task 7)
    - LFS bundle covers ≥30 days of v1 cycle history (count manually; operator records in PR description)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 (S-02 + S-03): Bench Jetson 1-week clean soak + handoff gate</name>
  <files></files>
  <read_first>
    - .planning/phases/05-bench-field-shadow/SOAK_RUNBOOK.md § 1 (soak windows), § 2 (daily operator checks), § 3 (F-04 violation disposition)
    - .planning/phases/05-bench-field-shadow/05-CONTEXT.md (S-01, S-02, S-03 definitions)
  </read_first>
  <what-built>
    Nothing automated. The bench Jetson runs v2 at canonical paths (no shadow mode)
    for 7 consecutive days. The Plan 04-07 HIL nightly cron exercises fault
    scenarios automatically during this window (F-02).
  </what-built>
  <how-to-verify>
    Operator checklist for the bench week:

    1. Confirm v2 is running on the bench Jetson at canonical paths:
       ```
       systemctl status spark-modem-watchdog.service
       sudo cat /var/lib/spark-modem-watchdog/status.json | jq '.cycle.iter'
       ```
       The daemon should be `active (running)` with `cycle.iter` incrementing.

    2. Record bench soak START timestamp (ISO-8601 UTC). Transcribe later into
       SIGNOFF.md "Bench soak start" field.

    3. **Every day for 7 consecutive days**, execute SOAK_RUNBOOK.md § 2 daily
       checks (2.1 daemon health, 2.2 cycle health, 2.3 state scan, 2.4 action
       history, 2.5 RSS tripwire). Save each day's evidence (journalctl slice,
       status.json snapshot, history slice) in a dated directory:
       ```
       mkdir -p phase5-evidence/bench/day-<N>
       sudo cat /var/lib/spark-modem-watchdog/status.json > phase5-evidence/bench/day-<N>/status.json
       sudo journalctl -u spark-modem-watchdog.service --since "24 hours ago" \
         > phase5-evidence/bench/day-<N>/journal.log
       ...
       ```

    4. **On any anomaly:** execute the F-04 violation disposition workflow
       (SOAK_RUNBOOK.md § 3). Record EVERY anomaly in a draft of SIGNOFF.md
       F-04 violations log immediately, even before classification is finalized.

    5. Monitor the Plan 04-07 HIL nightly run each morning:
       ```
       gh run list --workflow=hil.yml --limit=10
       ```
       A failed nightly run during the bench week is an F-04 event (counts
       against the 1-minor/week budget for whichever S-01 gate the failure
       maps to).

    6. **S-03 handoff gate** — at the end of the 7th day, run the soak-exit
       audits scoped to the bench week:
       ```
       python tools/audit_soak_zao.py \
         --events /var/log/spark-modem-watchdog/events.jsonl \
         --zao-log /var/log/zao-remote-endpoint.log \
         --since-iso <BENCH_SOAK_START_ISO> \
         --out phase5-evidence/bench/audit-zao.json

       python tools/audit_soak_exhausted.py \
         --events /var/log/spark-modem-watchdog/events.jsonl \
         --since-iso <BENCH_SOAK_START_ISO> \
         --out phase5-evidence/bench/audit-exhausted.json
       ```
       Both must exit 0 (OR exit 1 with at most 1 violation per gate that is
       within the F-04 budget AND dispositioned).

    7. Daemon-crash count (S-01 #1) over bench week:
       ```
       sudo journalctl -u spark-modem-watchdog.service --since <BENCH_SOAK_START_ISO> \
         | grep -c "Main process exited\|terminated abnormally\|reason=CRASH" || echo 0
       ```
       Must be 0, or within F-04 budget AND dispositioned.

    8. Record bench soak END timestamp. If gates are clean (or within F-04
       budget): mark S-03 handoff PASSED in your notes. If a 2nd violation
       of the same gate occurred in the same week: bench soak clock resets;
       repeat Tasks 2 from step 1.

    9. **DO NOT proceed to Task 3 (field deploy) until S-03 handoff is PASSED.**
       This is the bench → field gate per CONTEXT.md S-03.
  </how-to-verify>
  <resume-signal>
    Type "approved: bench week S-03 PASSED, started <ISO>, ended <ISO>, F-04
    violations: <N>" once the 7-day bench soak has completed cleanly. Provide
    the ISO timestamps + violation count for SIGNOFF.md transcription. Resume
    to Task 3.

    If the bench week failed S-03 (2nd violation reset): type "RESET: <reason>"
    and re-run Task 2 from step 1.
  </resume-signal>
  <acceptance_criteria>
    - 7 consecutive calendar days of operator daily-check evidence saved under `phase5-evidence/bench/day-1/` ... `phase5-evidence/bench/day-7/`
    - `phase5-evidence/bench/audit-zao.json` exists with `violations=0` (or ≤ F-04 budget)
    - `phase5-evidence/bench/audit-exhausted.json` exists with `violations=0` (or ≤ F-04 budget)
    - Daemon-crash count over bench window is 0 (or ≤ F-04 budget)
    - Bench soak start + end ISO timestamps recorded in resume-signal for SIGNOFF.md transcription
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 (S-02): Field box 2-week clean soak (natural faults only — F-01)</name>
  <files></files>
  <read_first>
    - SOAK_RUNBOOK.md § 1, § 2, § 3
    - 05-CONTEXT.md F-01 (no field-box synthetic injection), F-03 (no natural-fault minimum)
  </read_first>
  <what-built>
    Nothing automated. v2 runs on ONE field box at canonical paths for 14
    consecutive days. NO synthetic fault injection on the field box (F-01).
    Natural faults are observed and recorded; F-03 means no minimum fault
    count is required for exit.
  </what-built>
  <how-to-verify>
    Operator checklist for the field 2 weeks:

    1. Confirm v2 is running on the field box at canonical paths (same checks
       as Task 2 step 1).

    2. Record field soak START timestamp (ISO).

    3. **Every day for 14 consecutive days**, execute the daily checks (same
       as Task 2 step 3) and save evidence under `phase5-evidence/field/day-<N>/`.

    4. **No synthetic injection on the field box (F-01).** Do NOT run
       `tests/hil/fault_inject.py`, do NOT trigger `pkill -9 qmi-proxy`, do
       NOT manipulate `/sys/class/usb_device/.../bConfigurationValue`. The
       field box rides natural faults only.

    5. Track natural-fault events as they occur (informational only per F-03;
       no minimum required for exit):
       ```
       sudo spark-modem ctl history --since=14d \
         | jq -c 'select(.kind=="action_planned")' \
         > phase5-evidence/field/natural-fault-actions.jsonl
       ```

    6. On any anomaly: F-04 disposition workflow (SOAK_RUNBOOK § 3); record
       in draft SIGNOFF.md F-04 log immediately.

    7. **End of day 14** — run the soak-exit audits scoped to the field
       window (same as Task 2 step 6, with `--since-iso=<FIELD_SOAK_START_ISO>`).
       Save outputs at `phase5-evidence/field/audit-zao.json` and
       `phase5-evidence/field/audit-exhausted.json`.

    8. Daemon-crash count over field window: zero (or ≤ F-04 budget per
       week — 2 violations of S-01 #1 in the 14-day window total budget).

    9. Record field soak END timestamp.

    10. If a 2nd violation of the same gate occurred in the same week:
        field soak clock resets that week; the engineer judges whether to
        restart the entire 14-day window or only the week-with-resets (per
        CONTEXT.md F-04 + S-02 the conservative reading is restart the full
        field 14 days, but the engineer's judgment governs — record the
        decision in SIGNOFF.md free-text rationale).
  </how-to-verify>
  <resume-signal>
    Type "approved: field 2 weeks PASSED, started <ISO>, ended <ISO>, F-04
    violations: <N>". Resume to Task 4.

    On reset: "RESET: <reason>" and re-run Task 3.
  </resume-signal>
  <acceptance_criteria>
    - 14 consecutive calendar days of evidence saved under `phase5-evidence/field/day-1/` ... `day-14/`
    - `phase5-evidence/field/audit-zao.json` exists with `violations` ≤ F-04 budget AND every violation dispositioned in draft SIGNOFF.md
    - `phase5-evidence/field/audit-exhausted.json` exists with `violations` ≤ F-04 budget AND every violation dispositioned
    - Field soak start + end ISO timestamps recorded
    - No synthetic-injection artifacts under `phase5-evidence/field/` (operator attests F-01 honored)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4 (X-04): Fleet-fixture capture sweep across every Phase 6 box</name>
  <files>
    tests/fixtures/fleet/
  </files>
  <read_first>
    - .planning/phases/05-bench-field-shadow/05-CONTEXT.md X-01, X-02, X-04
    - src/spark_modem/cli/ctl/capture_fleet_fixture.py (the CLI verb — Plan 03)
    - SOAK_RUNBOOK.md (no specific section; the runbook does NOT cover X-04 because the capture sweep happens during the physical-access window, not the soak proper)
  </read_first>
  <what-built>
    Nothing automated by Claude. The on-site engineer runs `spark-modem ctl
    capture-fleet-fixture` on every box destined for Phase 6 cutover, during
    the physical-access window. Per CONTEXT.md X-04 the sweep happens after
    the soaks complete (so the bench + field boxes are also captured at this
    stage even though they've been running v2 all along — the resulting
    triple.json files prove the homogeneous-fleet assumption).
  </what-built>
  <how-to-verify>
    Operator checklist:

    1. List every Phase 6 cutover-candidate box. Include bench Jetson + field
       box even though they ran the soaks; they need triple.json in the
       known-fleet index too.

    2. For EACH box (one per access session):
       a. SSH or physical-console access.
       b. Run the capture command:
          ```
          sudo spark-modem ctl capture-fleet-fixture --out=/tmp/fleet-fixture-<box-id>
          ```
          (Note: the verb does NOT require the daemon to be running per Plan
          03's X-03 chicken-and-egg fix. If the daemon is running, capture
          still works — it does not contend on the PID lock.)
       c. Verify the output tree shape:
          ```
          ls /tmp/fleet-fixture-<box-id>/
          # expected: triple.json  qmi/  zao-log-sample.txt
          ls /tmp/fleet-fixture-<box-id>/qmi/
          # expected: one subdir per modem, named with usb_path (e.g. 2-3.1.1)
          ```
          USB-path-keyed subdirs (ADR-0009 invariant); if you see `cdc-wdm0`
          → bug, file an issue + halt.
       d. Verify PII redaction in the captured stdout:
          ```
          grep -rE '[0-9]{18,22}' /tmp/fleet-fixture-<box-id>/qmi/ | grep -v '<redacted:'
          ```
          ZERO matches expected. Non-zero → STOP, do not commit; raise an
          issue against `redact_pii_from_raw_qmicli` (Plan 03).
       e. Copy to dev laptop + place under `tests/fixtures/fleet/<box-id>/`:
          ```
          # On dev laptop:
          scp -r <jetson>:/tmp/fleet-fixture-<box-id> tests/fixtures/fleet/<box-id>
          ```

    3. After ALL boxes captured, open ONE batched Phase-6-prereq PR:
       ```
       git add tests/fixtures/fleet/
       git commit -m "phase 5: X-04 fleet-fixture batch (N boxes)"
       gh pr create --title "phase 5: X-04 fleet-fixture batch" --body "..."
       ```

    4. **PR must merge before opening the Phase 6 entry PR.** The Plan 06
       debian/install line ships these fixtures into the .deb; the Phase 6
       cutover .deb must contain a triple.json for every Phase-6 box or the
       X-03 preflight will refuse to start (Plan 04).

    5. Record in SIGNOFF.md "X-04 Fleet fixtures captured" checklist: tick
       each box; record the batched-PR URL.
  </how-to-verify>
  <resume-signal>
    Type "approved: X-04 batched PR merged at <short-sha>, N=<count> boxes
    captured". Resume to Task 5.
  </resume-signal>
  <acceptance_criteria>
    - `ls tests/fixtures/fleet/` contains one subdir per Phase 6 cutover box (count >= number of fleet boxes including bench + field)
    - Each subdir contains `triple.json` deserialisable as FleetTriple (em7421_firmware + zao_sdk + libqmi all strings)
    - `find tests/fixtures/fleet/ -name 'triple.json' -exec python -c "import json,sys; d=json.load(open(sys.argv[1])); assert all(k in d for k in ('em7421_firmware','zao_sdk','libqmi')), sys.argv[1]" {} \;` exits 0 for every file
    - `grep -rE '[0-9]{18,22}' tests/fixtures/fleet/ | grep -v '<redacted:'` returns ZERO matches (no raw PII)
    - `find tests/fixtures/fleet/ -type d -name 'qmi'` contains only directories whose immediate children match `r"^\d+-\d+(\.\d+)+$"` (usb_path shape per ADR-0009)
    - X-04 batched PR merged; merge-commit SHA recorded in SIGNOFF.md
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5 (R-02): Replay-harness one-shot at Phase 5 exit</name>
  <files>
    .planning/phases/05-bench-field-shadow/replay-summary-phase5-exit.json
  </files>
  <read_first>
    - SOAK_RUNBOOK.md § 4.3 (R-02 procedure)
    - tests/replay/test_v1_agreement.py (the gate substrate)
    - tests/replay/conftest.py (the R-03 hard-fail threshold — 0.95)
  </read_first>
  <what-built>
    Nothing automated by Claude. The engineer triggers `pytest tests/replay/`
    once on the dev laptop AFTER the X-04 PR merges, against the freshly-pulled
    v1-30d bundle from Task 1.
  </what-built>
  <how-to-verify>
    Operator checklist:

    1. On the dev laptop, ensure the v1-30d bundle from Task 1 is checked out
       and LFS-resolved:
       ```
       git pull origin main
       git lfs pull
       ls -la tests/fixtures/replay/v1-30d/
       ```

    2. Run the replay-harness:
       ```
       pytest tests/replay/test_v1_agreement.py -v --tb=short 2>&1 | tee replay-output.log
       ```
       The R-03 conftest hook hard-fails the run at fault-cycle agreement <
       0.95. Test PASS implies agreement ≥ 0.95.

    3. Locate the pytest_sessionfinish JSON:
       ```
       ls -la artifacts/replay-summary.json
       cat artifacts/replay-summary.json | jq '.fault_cycle_agreement'
       ```
       Expected: a float ≥ 0.95.

    4. Copy to phase directory with the exit-artifact name:
       ```
       cp artifacts/replay-summary.json \
          .planning/phases/05-bench-field-shadow/replay-summary-phase5-exit.json
       ```

    5. If the run FAILS (agreement < 0.95): do NOT proceed. Investigate the
       disagreements in the JSON details; either:
       a. Bug-fix in v2 policy + re-run (most disagreements at this stage
          would be Phase 4 regressions surfaced by the fresh bundle), OR
       b. File a Phase 6 entry blocker; SIGNOFF.md cannot be approved.

    6. Record the agreement rate for SIGNOFF.md "R-02 Replay-harness gate"
       field.
  </how-to-verify>
  <resume-signal>
    Type "approved: R-02 PASS, fault-cycle agreement = <X.XX>". Resume to Task 6.
    On fail: "FAIL: <agreement-rate>; root cause: <summary>" and either iterate
    on v2 + re-run, or escalate.
  </resume-signal>
  <acceptance_criteria>
    - `pytest tests/replay/test_v1_agreement.py` exit code is 0
    - `.planning/phases/05-bench-field-shadow/replay-summary-phase5-exit.json` exists
    - `python -c "import json; d=json.load(open('.planning/phases/05-bench-field-shadow/replay-summary-phase5-exit.json')); rate=d.get('fault_cycle_agreement', 0); assert rate >= 0.95, rate"` exits 0
    - Agreement rate recorded in SIGNOFF.md R-02 field (transcribed at Task 7)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 6 (S-04): SIGNOFF.md fill + commit + Phase 6 entry PR</name>
  <files>
    .planning/phases/05-bench-field-shadow/SIGNOFF.md
    .planning/phases/05-bench-field-shadow/replay-summary-phase5-exit.json
    artifacts/soak-zao-violations.json
    artifacts/soak-exhausted-violations.json
  </files>
  <read_first>
    - .planning/phases/05-bench-field-shadow/SIGNOFF.md (the template Plan 07 shipped — every blank slot the engineer fills)
    - SOAK_RUNBOOK.md § 4.4 (the commit procedure)
    - Task 1 / 2 / 3 / 4 / 5 resume-signal records (recorded ISO timestamps + counts + SHAs that get transcribed into SIGNOFF.md)
  </read_first>
  <what-built>
    Nothing new. The engineer fills the SIGNOFF.md template with the evidence
    accumulated through Tasks 1-5, then commits SIGNOFF.md + the three
    machine-readable artifacts in one PR.
  </what-built>
  <how-to-verify>
    Operator checklist:

    1. Open SIGNOFF.md in the editor.

    2. Fill the header front-matter table:
       - Engineer name
       - Bench Jetson box-id (from Task 2)
       - Field box box-id (from Task 3)
       - Bench soak start/end ISO (from Task 2)
       - Field soak start/end ISO (from Task 3)

    3. Fill the S-01 Exit Gates table — three rows, two columns each:
       - Row #1 (M6): tick ✅ if `audit-zao.json` violations==0 in BOTH bench AND
         field windows (or ≤F-04 budget AND dispositioned).
       - Row #2 (ADR-0003): tick ✅ if `phase5-evidence/bench/audit-zao.json` AND
         `phase5-evidence/field/audit-zao.json` show violations==0.
       - Row #3 (M4): tick ✅ if both `audit-exhausted.json` files show
         violations==0.

    4. Fill R-02 Replay-harness gate:
       - Agreement rate (from Task 5)
       - PASS (always — failing this means we did not reach Task 6)
       - Artifact path
       - Bundle source: the R-01 merge-commit SHA from Task 1

    5. Fill S-01.1 informational metrics:
       - Cycle P99 (read from `phase5-evidence/*/status.json` or Prom scrape data)
       - RSS (same source)
       - These are informational; record them even if they exceed targets
         (Phase 6 will look at them, not Phase 5 gate)

    6. Fill F-04 Violations log:
       - One row per violation, regardless of disposition
       - Columns: date, gate, classification (minor/non-minor), root cause,
         issue+PR links, customer-outage? (yes/no)
       - If NO violations recorded, note "none recorded yet."

    7. Fill X-04 Fleet fixtures captured checklist:
       - Tick each box
       - Batched PR link (from Task 4)

    8. Fill free-text rationale (≤1000 words):
       - What you observed during both soak windows
       - Why you're comfortable signing off
       - Any concerns flagged for Phase 6 (carrier-table tunings,
         signal-floor revisits, etc.)

    9. Fill Phase 6 entry approval:
       - All four boxes ticked
       - Engineer signature / commit author
       - Date ISO

    10. Commit + open Phase 6 entry PR:
        ```
        git add .planning/phases/05-bench-field-shadow/SIGNOFF.md \
                .planning/phases/05-bench-field-shadow/replay-summary-phase5-exit.json \
                artifacts/soak-zao-violations.json \
                artifacts/soak-exhausted-violations.json
        git commit -m "phase 5: SIGNOFF.md + replay-harness + audit JSONs (Phase 6 entry)"
        gh pr create --title "phase 5: SIGNOFF — Phase 6 entry" --body "..."
        ```

    11. **Reviewer enforces:** PR cannot merge until all four "Phase 6 entry
        approval" boxes are checked AND the X-04 batched PR has already merged.

    12. On merge, Phase 5 is COMPLETE. Phase 6 (`/gsd-plan-phase 6`) can begin.
  </how-to-verify>
  <resume-signal>
    Type "approved: SIGNOFF.md merged at <short-sha>, Phase 5 COMPLETE" once
    the Phase 6 entry PR has merged. This is the final Phase 5 exit signal.
  </resume-signal>
  <acceptance_criteria>
    - `.planning/phases/05-bench-field-shadow/SIGNOFF.md` filled (no `_engineer fills here_` placeholders remain except the deliberate free-text section)
    - `grep -E '^\| \\| ' .planning/phases/05-bench-field-shadow/SIGNOFF.md` returns 0 lines (no blank rows in the S-01 Exit Gates / S-01.1 metrics / F-04 violations tables — every cell populated)
    - All four checkboxes in "Phase 6 entry approval" section are ticked (☒ or ✅)
    - `.planning/phases/05-bench-field-shadow/replay-summary-phase5-exit.json` exists and has `fault_cycle_agreement >= 0.95`
    - `artifacts/soak-zao-violations.json` exists (bench + field merged or one per window per engineer choice)
    - `artifacts/soak-exhausted-violations.json` exists
    - The Phase 6 entry PR has merged; its merge-commit SHA is the resume-signal payload
    - `git log --oneline .planning/phases/05-bench-field-shadow/SIGNOFF.md | head -1 | grep "phase 5: SIGNOFF\|Phase 6 entry"` returns 1 match
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's daily-check evidence under `phase5-evidence/` | Local-only; may contain pre-redaction journalctl output. Operator's judgement governs sensitivity. |
| Engineer-authored SIGNOFF.md → Phase 6 entry PR review | Reviewer audit is the second gate; SIGNOFF cannot pass review without committed JSON artifacts matching the markdown values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Information disclosure | Operator commits `phase5-evidence/` containing raw ICCID/IMSI from journalctl into git | accept | `phase5-evidence/` is a working directory; SOAK_RUNBOOK.md does NOT instruct the operator to commit it. The committed artifacts (SIGNOFF.md + replay-summary + audit JSONs + fleet fixtures) are all redacted at source by Plan 03 / Plan 05. Operator-judgment threat; not template-enforced. |
| T-05-08-02 | Repudiation | Engineer marks gates PASS without running the audit tools | mitigate | Acceptance criteria require the audit JSON files to be committed alongside SIGNOFF.md (Task 6 step 10); PR reviewer verifies the JSON contents match the SIGNOFF.md ticks. |
| T-05-08-03 | Tampering | Engineer manually edits `replay-summary-phase5-exit.json` to inflate the agreement rate | mitigate | The pytest_sessionfinish hook writes the JSON; manual edits would break the schema validation Phase 6 entry can pin against. SOAK_RUNBOOK.md Section 4.3 documents the canonical procedure as `cp artifacts/replay-summary.json` (no edit). |
| T-05-08-04 | Denial of service | Field box 14-day soak repeatedly resets due to F-04 violations, blocking Phase 6 indefinitely | accept | This is the gate working as designed; the right response is to fix the underlying v2 defect, not to relax the gate. F-04 budget is deliberately tight (1 minor/week). |
| T-05-08-05 | Spoofing | Wrong v1-30d bundle used for R-02 (engineer uses a stale bundle instead of the day-1 fresh pull) | mitigate | SIGNOFF.md "Bundle source" field records the R-01 merge-commit SHA (Task 1 resume-signal); PR reviewer cross-checks against git log on `tests/fixtures/replay/v1-30d/`. |
| T-05-08-06 | Privilege | Engineer runs synthetic injection on field box, violating F-01 | mitigate | SOAK_RUNBOOK.md § 3 explicitly says "do NOT run tests/hil/fault_inject.py"; SIGNOFF.md acceptance criterion requires the engineer to attest F-01 honored (no synthetic-injection artifacts under `phase5-evidence/field/`). |
</threat_model>

<verification>
- All six tasks completed sequentially; each resume-signal captured the evidence needed for SIGNOFF.md transcription
- SIGNOFF.md committed at Phase 5 exit with all four "Phase 6 entry approval" boxes ticked
- replay-summary-phase5-exit.json shows fault_cycle_agreement ≥ 0.95
- X-04 batched PR merged before SIGNOFF.md PR opened
- Audit JSONs committed alongside SIGNOFF.md prove the S-01 gate ticks
</verification>

<success_criteria>
1. R-01: tests/fixtures/replay/v1-30d/ LFS PR merged before bench soak begins.
2. S-02 / S-03: Bench Jetson 1-week clean soak completed (gates within F-04 budget, all dispositioned); S-03 handoff PASSED.
3. S-02: Field box 2-week clean soak completed (same gates).
4. X-04: All fleet boxes' triple.json committed under `tests/fixtures/fleet/<box-id>/` via the batched Phase-6-prereq PR (merged).
5. R-02: pytest tests/replay/test_v1_agreement.py against the freshly-pulled v1-30d bundle achieves ≥ 0.95 fault-cycle agreement; `replay-summary-phase5-exit.json` committed.
6. S-04: SIGNOFF.md filled + committed at Phase 5 exit; all four "Phase 6 entry approval" boxes ticked; F-04 violations log audit-trailed.
7. Phase 6 entry PR can now be opened (the actual /gsd-plan-phase 6 work is OUT of Phase 5 scope).
</success_criteria>

<output>
After completion (i.e. after Phase 5 exit), create `.planning/phases/05-bench-field-shadow/05-08-SUMMARY.md` capturing:
- Bench soak ISO start + end + any F-04 violations
- Field soak ISO start + end + any F-04 violations
- X-04 batched PR merge SHA + box count
- R-02 fault-cycle agreement rate
- SIGNOFF.md merge SHA + Phase 5 calendar duration (kickoff → SIGNOFF merge)
- Any deferrals carried into Phase 6 (e.g. carrier-table tunings; F-04-dispositioned PRs that did not merge by Phase 5 exit)
</output>
