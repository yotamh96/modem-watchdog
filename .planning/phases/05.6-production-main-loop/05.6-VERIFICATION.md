---
phase: 05.6-production-main-loop
verified: 2026-05-17T00:00:00Z
status: human_needed
score: 14/14 must-haves verified
overrides_applied: 0
human_verification:
  - id: 05.6-05-bench-jetson-sc-1-7
    test: "Run the 7-row SC checklist on a bench Jetson with HMAC secret + known-fleet entry provisioned per Phase 05.1 EXIT-CHECKLIST.md rows 1-3. Script is in 05.6-05-PLAN.md Task 2."
    expected: "All 7 acceptance criteria pass: (SC #1) systemctl start reaches Active: active (running) within 60s; (SC #2) after 10min uptime zero ERROR/CRITICAL journal lines, status.json cycle_index >=10, metrics.sock returns valid Prometheus text; (SC #3) systemctl stop returns 0 in <5s with daemon_stopped reason=sigterm and clean-shutdown marker; (SC #4) systemctl reload applies RELOAD_DATA fields, refuses RELOAD_RESTART fields with restart_required log; (SC #5) cycle_duration_seconds_bucket le=10.0 >= total count (no P99 overflow); (SC #6) pytest test suite pass count unchanged plus new test PASSES; (SC #7) pytest tests/integration/test_production_main.py exits 0 on aarch64 runner."
    why_human: "SC #1-#7 require a real bench Jetson with systemd Type=notify, a real NOTIFY_SOCKET, the actual /run RuntimeDirectory semantics, real SIGTERM/SIGHUP choreography against the real signal stack, and the real Zao log + producer traffic. The in-process pytest (Task 1) catches regression but does NOT exercise the end-to-end systemd protocol. Deferred at execution time per operator decision — bench Jetson not available."
---

# Phase 05.6: production-main-loop Verification Report

**Phase Goal:** Replace the `_production_main` return-0 placeholder with real TaskGroup wiring (4 event producers + cycle driver + sd_notify lifecycle + signal handlers) — assemble, not build, since all modules pre-exist.
**Verified:** 2026-05-17
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | `Settings()` exposes a `cycle_interval_seconds: float` field defaulting to 60.0, marked RELOAD_DATA, with ge=1.0 constraint. | VERIFIED | `settings.py` line 86-91: `cycle_interval_seconds: float = Field(default=60.0, ge=1.0, json_schema_extra=RELOAD_DATA, ...)` between `ladder_min_interval_seconds` and `healthy_streak_decay_k`. |
| 2  | `WatchdogSec=180s` is the only watchdog value in the shipped unit file (3x the 60s production cycle). | VERIFIED | `debian/spark-modem-watchdog.service` line 39: `WatchdogSec=180s`; comment cites C-03 and Phase 05.6; `WatchdogSec=90s` is absent. |
| 3  | `_production_main` no longer ends in a `return 0` placeholder; it enters an `asyncio.TaskGroup` with SIGTERM/SIGHUP handlers installed via `loop.add_signal_handler` BEFORE the TaskGroup. | VERIFIED | `main.py` lines 383-384: `loop.add_signal_handler(signal.SIGTERM, ...)` and `loop.add_signal_handler(signal.SIGHUP, ...)` precede the TaskGroup at line 917. The two `return 0` lines in the file are: one in `_laptop_main` (line 259) and one after the TaskGroup unwinds in `_production_main` (line 987). Zero placeholder `return 0` inside the lock before the TaskGroup. |
| 4  | The TaskGroup in `_production_main` spawns 4 supervised event-source producers (udev, rtnetlink, asyncinotify, kmsg) each wrapped in `restart_on_crash(name, factory, sleeper, event_logger, clock)`. | VERIFIED | `main.py` lines 919-960: four `producer_tasks.append(tg.create_task(restart_on_crash("udev_producer"/"rtnetlink_producer"/"asyncinotify_producer"/"kmsg_producer", ...)))` with `sleeper=sleeper, event_logger=supervisor_event_logger, clock=clock`. |
| 5  | All 4 producer tasks accumulate into a `list[asyncio.Task[object]]` named `producer_tasks` accessible to `_sigterm_watcher`. | VERIFIED | `main.py` line 444: `producer_tasks: list[asyncio.Task[object]] = []` declared outside the TaskGroup. Four `producer_tasks.append(...)` calls at lines 918, 929, 940, 951. Passed as `producer_tasks=producer_tasks` to `SigtermChoreography`. |
| 6  | Producers push onto a shared `asyncio.Queue[WakeSignal]` (`event_queue`) with maxsize=1024, created before TaskGroup entry. | VERIFIED | `main.py` line 393: `event_queue: asyncio.Queue[WakeSignal] = asyncio.Queue(maxsize=1024)`. All 4 producer factories call `event_queue=cast(Any, event_queue)`. |
| 7  | All 4 producer factories are dependency-injectable via optional `_production_main` keyword-only parameters (all default None). | VERIFIED | `main.py` lines 264-270: `inventory_factory`, `zao_factory`, `udev_factory`, `rtnetlink_factory`, `asyncinotify_factory`, `kmsg_factory` all keyword-only, all `| None = None`. |
| 8  | The asyncinotify producer is wired with canonical Zao log path `/var/log/zao-remote-endpoint.log` and `settings.events_log_path`. | VERIFIED | `main.py` line 425: `zao_log_path = Path("/var/log/zao-remote-endpoint.log")`. Line 639: `_asyncinotify_factory_default` calls `run_asyncinotify_producer(event_queue=..., events_jsonl_path=event_log_path, zao_log_path=zao_log_path, ...)`. |
| 9  | The stub `_cycle_loop` from plan 05.6-01 is replaced by the production body: `CycleDriver.run_one_cycle` -> WATCHDOG -> STATUS -> READY (cycle 0 only); scheduler wakes the cycle via event_queue or deadline. | VERIFIED | `main.py` lines 703-810: `async def _cycle_loop()` with `await driver.run_one_cycle(cycle_id=cycle_id)` at line 770, `sd.watchdog_kick()` at line 788, `sd.status(...)` at lines 800-803, `sd.ready(...)` at line 807 (inside `if cycle_id == 0:`), `scheduler.advance()` at line 810. Cycle 0 drops straight in (no wait). |
| 10 | Production subsystems are constructed once: `StateStore`, `MetricRegistry`, `WebhookPoster` (HMAC via `resolve_hmac_secret_path`), `CarrierTable` (from `settings.carriers_yaml_path`), `CycleScheduler(interval_seconds=settings.cycle_interval_seconds)`, `CycleDriver`. | VERIFIED | `main.py` lines 514-596: `StateStore(...)`, `MetricRegistry()`, `_load_carrier_table_yaml(...)`, `CycleScheduler(interval_seconds=settings.cycle_interval_seconds, clock=clock)`, `_read_hmac_secret(settings.resolve_hmac_secret_path())`, `WebhookPoster(...)`, `CycleDriver(...)`. |
| 11 | Prometheus UDS server starts BEFORE cycle 0 via `asyncio.to_thread(prom_server.serve_forever)` as a TaskGroup member; `DaemonRestart` boot envelope is enqueued BEFORE TaskGroup entry. | VERIFIED | `main.py` line 605: `prom_server = start_metrics_server(...)` constructed before TaskGroup. Line 970: `tg.create_task(asyncio.to_thread(prom_server.serve_forever))`. Lines 571-578: `boot_envelope = WebhookEnvelope(...)` + `await webhook_poster.enqueue(boot_envelope)` before the TaskGroup. |
| 12 | `_sigterm_watcher` constructs and runs `SigtermChoreography` with `deadline_seconds=5.0`; clean-shutdown marker written at step 8; `webhook_poster.stop()` is idempotent post-TaskGroup. | VERIFIED | `main.py` lines 831-863: nested `async def _sigterm_watcher()` waits on `shutdown_event`, reads `cycle_task_ref[0]`, constructs `SigtermChoreography(cycle_driver_task=..., producer_tasks=..., webhook_poster=..., event_logger=..., metrics_socket_path=..., run_dir=..., clock=..., boot_monotonic=..., cycle_count_ref=..., state_flush=None)`, calls `choreography.execute(deadline_seconds=5.0)`. Line 986: `webhook_poster.stop()` after TaskGroup unwind. |
| 13 | `_sighup_watcher` constructs `SighupSwapper` with `_SettingsRef` cell + `settings_factory=Settings` + `dns_cache=webhook_poster._dns_cache`; RELOAD_DATA applies, RELOAD_RESTART refuses. | VERIFIED | `main.py` lines 473-484: nested `class _SettingsRef` with `get/set`. Lines 561-565: `SighupSwapper(settings_ref=settings_ref, settings_factory=Settings, dns_cache=webhook_poster._dns_cache)`. Lines 876-906: `async def _sighup_watcher()` races sighup/shutdown via `asyncio.wait(FIRST_COMPLETED)`, clears event, calls `sighup_swapper.try_apply_reload()`. |
| 14 | `tests/integration/test_production_main.py` exists with `pytestmark=[linux_only, skipif(win32), asyncio]`, routes through `await main(["--skip-preflight"])`, monkeypatches FakeSdNotify + idle producers + fake inventory + isolated MetricRegistry + fake prom server, drives shutdown via `signal.raise_signal(SIGTERM)`, and asserts rc==0 / 1 READY call / >=1 watchdog / status.json with 4 modems / cycle_duration_seconds_count>=1. | VERIFIED | `tests/integration/test_production_main.py` lines 34-41: pytestmark triple. Line 258: `await main(["--skip-preflight"])` inside `_run_main()`. Lines 151-206: 8 monkeypatch calls. Lines 242+247: `signal.raise_signal(signal.SIGTERM)`. Lines 268-297: all T-04 assertions present including FR-61 PID lock assertion at line 239. |

**Score:** 14/14 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/spark_modem/config/settings.py` | `cycle_interval_seconds: float = Field(default=60.0, ge=1.0, json_schema_extra=RELOAD_DATA)` | VERIFIED | Line 86-91. Between `ladder_min_interval_seconds` and `healthy_streak_decay_k` as specified. |
| `debian/spark-modem-watchdog.service` | `WatchdogSec=180s` with refreshed U-04 comment block citing C-03 | VERIFIED | Lines 33-39. Comment references C-03 and Phase 05.6. `RuntimeDirectoryPreserve=yes`, `LoadCredential=`, and `Type=notify` all unchanged. |
| `src/spark_modem/daemon/main.py` | Production `_production_main` with TaskGroup, signal handlers, 4 producers, production cycle body, SigtermChoreography, SighupSwapper | VERIFIED | 1012-line file. No placeholder `_ = ...` keep-alive lines. No `signal.signal(` calls. No `MonitorObserver`. No `gather(return_exceptions=True)`. `asyncio.TaskGroup` present. 9 tasks: 4 producers + webhook + prom + cycle + sigterm + sighup. |
| `tests/integration/test_production_main.py` | In-process `await main(...)` regression test, 80+ lines, linux_only + asyncio marks | VERIFIED | 299-line file. All assertions present. `os.kill` absent (only in a comment). `signal.raise_signal` at lines 242+247. `monkeypatch.setattr(main_module, "SdNotifyLifecycle", ...)` at line 151. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `main.py (_production_main)` | `settings.py` | `settings.cycle_interval_seconds` read in `CycleScheduler(interval_seconds=...)` | VERIFIED | Line 533: `CycleScheduler(interval_seconds=settings.cycle_interval_seconds, clock=clock)`. |
| `main.py (_production_main)` | `daemon/lifecycle.py` | `sd.ready(...)` after cycle 0 in `_cycle_loop` | VERIFIED | Line 807: `sd.ready(f"first cycle ok healthy={healthy_count}/4")` inside `if cycle_id == 0:`. |
| `debian/spark-modem-watchdog.service` | `settings.py` | WatchdogSec=180s tracks the 60s `cycle_interval_seconds` default (3x ratio) | VERIFIED | Unit file line 39: `WatchdogSec=180s`. Comment at line 33 explicitly notes the 3x ratio. |
| `main.py (_production_main)` | `event_sources/supervisor.py` | `restart_on_crash(name, factory, sleeper=..., event_logger=..., clock=...)` for 4 producers | VERIFIED | Lines 919-960: four `restart_on_crash(...)` calls, each with name string, supervised wrapper, and the three keyword args. |
| `main.py (_production_main)` | `daemon/sigterm.py` | `SigtermChoreography(...).execute(deadline_seconds=5.0)` in `_sigterm_watcher` | VERIFIED | Lines 847-859: `SigtermChoreography(...)` construction and `await choreography.execute(deadline_seconds=5.0)`. |
| `main.py (_production_main)` | `daemon/sighup.py` | `SighupSwapper(settings_ref=..., settings_factory=Settings, dns_cache=...).try_apply_reload()` | VERIFIED | Lines 561-564: `SighupSwapper(...)` construction. Line 901: `await sighup_swapper.try_apply_reload()`. |
| `main.py (_production_main)` | `daemon/cycle_driver.py` | `CycleDriver(...).run_one_cycle(cycle_id=N)` in `_cycle_loop` | VERIFIED | Line 585: `driver = CycleDriver(...)`. Line 770: `result = await driver.run_one_cycle(cycle_id=cycle_id)`. |
| `main.py (_production_main)` | `status_reporter/prom.py` | `start_metrics_server(settings.metrics_socket_path)` + `asyncio.to_thread(prom_server.serve_forever)` | VERIFIED | Lines 605-608: `start_metrics_server(...)`. Line 970: `tg.create_task(asyncio.to_thread(prom_server.serve_forever))`. |
| `main.py (_production_main)` | `webhook/poster.py` | `WebhookPoster(...).run_forever()` as TaskGroup task + boot envelope enqueued before TaskGroup | VERIFIED | Lines 545-552: `WebhookPoster(...)`. Line 578: `await webhook_poster.enqueue(boot_envelope)`. Line 965: `tg.create_task(webhook_poster.run_forever())`. |
| `test_production_main.py` | `daemon/main.py` | `await main(["--skip-preflight"])` via real entrypoint | VERIFIED | Line 258 in a thin `_run_main()` wrapper; line 260: `await asyncio.wait_for(_run_main(), timeout=15.0)`. |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `_cycle_loop` in `main.py` | `result` from `driver.run_one_cycle(cycle_id)` | `CycleDriver` (Phase 2 module, fully implemented) | Yes — driver runs observe/policy/actions/persist pipeline | FLOWING |
| `status.json` | Written by `CycleDriver._write_status_report(...)` internally | `CycleDriver` at `cycle_driver.py:270` per SUMMARY | Yes — CycleDriver constructs from real modem state | FLOWING |
| `sd.ready(...)` | Called with `healthy_count` computed from `result.cycle_result.new_states` | `driver.run_one_cycle(...)` return value | Yes — populated from policy engine output | FLOWING |
| Integration test `status.json` | Written by `CycleDriver` during `await main(["--skip-preflight"])` | `_FakeFourModemInventory.scan()` -> `CycleDriver` | Yes — 4 `ModemDescriptor` instances flow into real cycle pipeline | FLOWING |

---

### Behavioral Spot-Checks

Step 7b is SKIPPED on Windows dev host. The test file is marked `linux_only` + `skipif(win32)` — the authoritative run target is the aarch64 self-hosted CI runner or a Linux dev host. On Windows, `start_metrics_server` raises `RuntimeError` by design and `signal.SIGHUP` does not exist. The 05.6-05-SUMMARY.md confirms the file passes ruff/format/ast-parse checks and will execute on Linux.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|------------|------------|-------------|--------|---------|
| FR-53 | 05.6-01, 05.6-04 | Graceful shutdown within 5s | SATISFIED | `SigtermChoreography.execute(deadline_seconds=5.0)` wired in `_sigterm_watcher`; clean-shutdown marker written at step 8. |
| FR-54 | 05.6-01, 05.6-04 | SIGHUP transactional config reload | SATISFIED | `SighupSwapper.try_apply_reload()` in `_sighup_watcher`; RELOAD_DATA applies, RELOAD_RESTART refuses with log. |
| FR-61 | 05.6-01, 05.6-04 | PID lock held for daemon lifetime | SATISFIED | `acquire_pid_lock(run_dir=run_dir)` wraps entire TaskGroup scope. Integration test asserts lock exists during cycle 0. |
| FR-75 | 05.6-01, 05.6-05 | READY=1 fires after first real cycle | SATISFIED | `sd.ready(...)` called only inside `if cycle_id == 0:` after `run_one_cycle` returns. |
| NFR-13 | 05.6-01 | READY within 60s | SATISFIED (automated) | Cycle 0 runs immediately (no wait on cycle 0); `sd.ready` follows. Bench Jetson timed validation is SC #1 (human). |
| FR-1 | 05.6-02 | udev USB event detection | SATISFIED | `run_udev_producer` wired with `restart_on_crash` into TaskGroup. |
| FR-3 | 05.6-02 | rtnetlink link-state detection | SATISFIED | `run_rtnetlink_producer` wired with `restart_on_crash`. |
| FR-4 | 05.6-02 | Zao log tailing | SATISFIED | `run_asyncinotify_producer` wired with canonical Zao log path. |
| FR-14 | 05.6-02 | kmsg host-issue detection | SATISFIED | `run_kmsg_producer` wired with `KmsgDedup(window_seconds=30.0)` and `_LoggingHostIssueEmitter`. |
| FR-2 | 05.6-03 | Cycle-driven modem observation | SATISFIED | `CycleDriver.run_one_cycle(cycle_id)` called every cycle. |
| FR-10 | 05.6-03 | State persisted per cycle | SATISFIED | `CycleDriver` writes state atomically internally; `StateStore` constructed against production paths. |
| FR-44 | 05.6-03 | Webhook boot envelope + per-cycle events | SATISFIED | `DaemonRestart` boot envelope enqueued before TaskGroup; `WebhookPoster.run_forever()` as TaskGroup task. |
| NFR-1 | 05.6-03 | P99 cycle <= 10s | NEEDS HUMAN | Bench Jetson SC #5 validates this under real modem + Zao traffic. |
| NFR-21 | 05.6-03 | Prometheus metrics served | SATISFIED | `start_metrics_server` bound before cycle 0; `asyncio.to_thread(prom_server.serve_forever)` in TaskGroup. |

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `src/spark_modem/daemon/main.py` | No `signal.signal(` calls — confirmed absent | Clean | CLAUDE.md anti-pattern not present |
| `src/spark_modem/daemon/main.py` | No `MonitorObserver` — confirmed absent | Clean | CLAUDE.md anti-pattern not present |
| `src/spark_modem/daemon/main.py` | No `gather(return_exceptions=True)` — confirmed absent | Clean | CLAUDE.md anti-pattern not present |
| `src/spark_modem/daemon/main.py` | `_LoggingHostIssueEmitter` placeholder for kmsg issue accumulator | Info | Plan 05.6-02 documents this as an intentional placeholder; plan 05.6-03 noted it would be replaced but scope was deferred. The emitter logs at INFO and truncates lines to 200 chars. It does NOT feed the CycleDriver's diag_builder in Phase 05.6 — per SPEC the accumulator wiring was deferred. The kmsg events are still observable via the journal. Not a blocker: the cycle still runs; host issues are logged. |
| `tests/integration/test_production_main.py` | `SPARK_MODEM_CYCLE_INTERVAL_SECONDS=0.1` — violates `ge=1.0` constraint in Settings | Info | The test uses `monkeypatch.setenv("SPARK_MODEM_CYCLE_INTERVAL_SECONDS", "0.1")`. This value is below the `ge=1.0` floor defined in Settings. However, pydantic-settings validation runs at Settings construction time inside `_production_main` via `Settings()`. IF validation rejects 0.1, the test would exit with rc=78. The SUMMARY for 05.6-01 documents that `ge=1.0` was honored over the plan's literal 0.1 example, but the test was written using 0.1 as the env override. This could be a test-level issue on a Linux runner — the test may fail with rc=78 rather than rc=0 if validation is strict. FLAGGED but cannot verify without running; needs Linux CI confirmation (SC #7). |

---

### Human Verification Required

The following items require operator action and cannot be verified programmatically.

#### 1. Bench Jetson SC #1..#7 — Final Phase Go/No-Go Gate

**Test:** On a bench Jetson with HMAC secret + known-fleet entry provisioned per Phase 05.1 EXIT-CHECKLIST.md rows 1-3, execute the 7-row checklist from `05.6-05-PLAN.md` Task 2:

- SC #1 (NFR-13 <=60s READY): `timeout 60 bash -c 'while ! systemctl is-active --quiet spark-modem-watchdog; do sleep 1; done'` + confirm journal contains READY=1.
- SC #2 (10min uptime health): after 600s — `is-active` = active, zero ERROR/CRITICAL journal lines, `status.json` cycle_index >=10, `curl --unix-socket .../metrics.sock http://localhost/metrics` returns valid Prometheus text with `cycle_duration_seconds_count >= 10`.
- SC #3 (FR-53 SIGTERM <=5s): `time sudo systemctl stop spark-modem-watchdog` returns 0 in <5s; `daemon_stopped reason=sigterm` in journal; clean-shutdown marker exists.
- SC #4 (FR-54 SIGHUP reload): RELOAD_DATA field change applies (logged); RELOAD_RESTART field change is refused with `restart_required` log; daemon stays active throughout.
- SC #5 (NFR-1 P99 cycle <=10s): `curl .../metrics | grep cycle_duration_seconds_bucket` — no overflow into `le="+Inf"`.
- SC #6 (regression): `pytest tests/ -q` reports same pass count as pre-phase-05.6 baseline.
- SC #7 (in-process integration test on aarch64): `pytest tests/integration/test_production_main.py -xvs` exits 0.

**Expected:** All 7 rows PASS. Resume signal: `"approved-7/7"` if all pass; otherwise describe which SC(s) failed with journal excerpt.

**Why human:** Requires a real systemd `Type=notify` boot with `NOTIFY_SOCKET`, real `/run` `RuntimeDirectory` semantics, real SIGTERM/SIGHUP signal delivery to a running daemon, and real Zao log + modem producer traffic. The in-process pytest (Task 1) pins the code wiring but cannot substitute for the end-to-end systemd protocol verification.

**Additional note on SC #7 / cycle_interval env var:** The test sets `SPARK_MODEM_CYCLE_INTERVAL_SECONDS=0.1` which is below the `ge=1.0` floor. On Linux CI / aarch64 runner, if pydantic-settings enforces the constraint at `Settings()` construction, `main()` will return rc=78 and the test will fail on `assert rc == 0`. The operator should confirm whether SC #7 passes cleanly or if the test needs the env var bumped to `1.0` (or a `--skip-settings-validation` seam added). This is an automated-check gap that only surfaces on a POSIX runner.

---

### Gaps Summary

No automated gaps found. All 14 must-have truths verified against the codebase. The phase goal — replacing the `_production_main` return-0 placeholder with real TaskGroup wiring — is accomplished in code.

One potential test-level issue is flagged in Anti-Patterns: the integration test uses `SPARK_MODEM_CYCLE_INTERVAL_SECONDS=0.1` which may fail pydantic-settings `ge=1.0` validation. This cannot be confirmed without a POSIX runner; it is surfaced as an item for the bench Jetson SC #7 verification.

The remaining gate is exclusively the bench Jetson human verification (SC #1..#7), deferred per operator decision at execution time. This is the intended `human_needed` outcome per the phase instructions.

---

_Verified: 2026-05-17_
_Verifier: Claude (gsd-verifier)_
