---
status: partial
phase: 05.6-production-main-loop
source: ["05.6-VERIFICATION.md", "05.6-05-PLAN.md"]
started: 2026-05-17T00:00:00Z
updated: 2026-05-17T00:00:00Z
---

## Current Test

[awaiting bench Jetson availability]

## Tests

### 1. SC #1 — NFR-13: READY ≤60s after boot
expected: |
  On a bench Jetson with HMAC secret + known-fleet entry provisioned
  per Phase 05.1 EXIT-CHECKLIST.md rows 1-3,
  `sudo apt install ./spark-modem-watchdog_2.0.*_arm64.deb` succeeds;
  `sudo systemctl start spark-modem-watchdog` causes
  `systemctl is-active` to return 0 within 60 seconds; the journal contains
  a `READY=1` indication (daemon logs `first cycle ok healthy=K/4`).
result: [pending]

### 2. SC #2 — status.json + Prom UDS valid after ≥10 minutes
expected: |
  After `sleep 600`: `systemctl is-active` returns `active`; no
  ERROR/CRITICAL journal lines; status.json `cycle_index ≥ 10`; curl on
  `/run/spark-modem-watchdog/metrics.sock` returns valid Prometheus text
  containing `cycle_duration_seconds_count >= 10`, `modem_state_value{modem=...}`,
  `actions_total{...}`.
result: [pending]

### 3. SC #3 — FR-53: SIGTERM ≤5s
expected: |
  `time sudo systemctl stop spark-modem-watchdog` exits 0 in <5s; journal
  contains a final `daemon_stopped reason=sigterm` event;
  `/run/spark-modem-watchdog/clean-shutdown` exists with
  `exit_reason: "sigterm"` (preserved via `RuntimeDirectoryPreserve=yes`).
result: [pending]

### 4. SC #4 — FR-54-runtime: SIGHUP transactional reload
expected: |
  RELOAD_DATA edit (e.g. `webhook_dedup_seconds: 120` in
  `/etc/spark-modem-watchdog/conf.d/99-test.yaml`) → `systemctl reload`
  logs `sighup: reload applied; changed_fields=['webhook_dedup_seconds']`;
  RELOAD_RESTART edit (e.g. `state_root: /tmp/nope`) → reload logs
  `sighup: restart_required fields changed=['state_root']`; daemon stays
  active throughout (no restart).
result: [pending]

### 5. SC #5 — NFR-1: P99 cycle ≤10s over ≥10 min uptime
expected: |
  `curl --unix-socket /run/spark-modem-watchdog/metrics.sock
  http://localhost/metrics | grep '^cycle_duration_seconds_bucket'` shows
  `cycle_duration_seconds_bucket{le="10.0"}` ≈ total count (no overflow
  into `le="+Inf"`).
result: [pending]

### 6. SC #6 — Regression: pytest pass count unchanged
expected: |
  On the aarch64 CI runner with the merged Phase 05.6 branch:
  `pytest tests/ -q` reports identical pass count to the pre-merge
  baseline AND adds the new
  `tests/integration/test_production_main.py::test_production_main_wires_taskgroup_and_fires_ready PASSED`.
result: [pending]

### 7. SC #7 — In-process integration test green on aarch64 CI
expected: |
  `pytest tests/integration/test_production_main.py -xvs` exits 0 on the
  aarch64 self-hosted CI runner. (Note: env-var
  `SPARK_MODEM_CYCLE_INTERVAL_SECONDS` was raised to "1.0" post-merge to
  honor Settings' `ge=1.0` floor — commit fix(05.6-05).)
result: [pending]

## Summary

total: 7
passed: 0
issues: 0
pending: 7
skipped: 0
blocked: 0

## Gaps

[None at this time. Resume signal: type "approved-7/7" via /gsd-verify-work 05.6
when bench Jetson is available, or describe which SC(s) failed with the
journal excerpt.]
