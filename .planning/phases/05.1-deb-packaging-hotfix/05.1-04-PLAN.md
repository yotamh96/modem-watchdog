---
phase: 05.1-deb-packaging-hotfix
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md
autonomous: true
requirements_addressed: []
# V-03 is the phase EXIT bar; no formal v1 REQ-ID maps.

must_haves:
  truths:
    - "EXIT-CHECKLIST.md exists at .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md"
    - "EXIT-CHECKLIST.md has a header table for box-id / .deb commit SHA / install + EXIT ISO timestamps (operator-fillable)"
    - "EXIT-CHECKLIST.md has a 9-row gate table covering the 9 V-03 steps from CONTEXT.md L-162..173"
    - "Each gate row has columns: # / Step / Status (☐ PASS ☐ FAIL) / Command / Expected / Observed / Notes"
    - "EXIT-CHECKLIST.md has an approval footer with engineer signature + ISO date (mirror SIGNOFF.md lines 109-119)"
    - "EXIT-CHECKLIST.md is template-only (placeholder values italicized with underscores, e.g. _bench-jetson-01_)"
  artifacts:
    - path: .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md
      provides: "Operator-facing 9-step exit checklist for the Phase 05.1 EXIT bar (V-03)"
      contains: "V-03 Exit Gates"
  key_links:
    - from: ".planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md"
      to: "Phase 05.1 EXIT bar (D-03)"
      via: "operator fills + commits this checklist; commit gates phase exit"
      pattern: "Approved by"

user_setup:
  - service: bench-jetson
    why: "EXIT-CHECKLIST execution requires the bench Jetson (V-03 EXIT bar)"
    env_vars: []
    dashboard_config:
      - task: "On-site engineer scp's built .deb to bench Jetson, runs dpkg -i, provisions HMAC secret, starts service, fills checklist, commits."
        location: "Bench Jetson (JetPack 5.1.5 / Ubuntu 20.04 / aarch64)"
---

<objective>
Author the operator-facing EXIT-CHECKLIST.md template that the on-site
engineer fills + commits as the Phase 05.1 EXIT gate. Mirror the shape of
Phase 5's SIGNOFF.md (CONTEXT.md decision V-03 explicitly invokes the same
pattern).

Implements locked decision **V-03** from
`.planning/phases/05.1-deb-packaging-hotfix/05.1-CONTEXT.md`.

Purpose: phase 05.1 has no ROADMAP-shaped Success Criteria block (D-03:
EXIT bar pattern, not a full SC block). The forcing function is a
structured operator-filled checklist with 9 rows — one per V-03 step from
CONTEXT.md L-162..173. The committed checklist is the phase-exit artifact.

This plan creates the TEMPLATE; the actual fill-in happens out-of-band on
the bench Jetson by the on-site engineer (single-operator workflow per
CONTEXT.md specifics).

Output: `.planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md` with
header table, 9-row gate table, approval footer, footer reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-deb-packaging-hotfix/05.1-CONTEXT.md
@.planning/phases/05.1-deb-packaging-hotfix/05.1-PATTERNS.md
@.planning/phases/05-bench-field-shadow/SIGNOFF.md

<interfaces>
<!-- The analog SIGNOFF.md is the canonical operator-checklist shape. -->

From .planning/phases/05-bench-field-shadow/SIGNOFF.md (full file ~124 lines) — header table pattern (lines 5-16), exit-gate table pattern (lines 27-31 with ☐ / ☐ columns), approval footer (lines 109-119), and the footer reference at lines 122-124:

```markdown
# Phase 5 Sign-off — Bench & Field Shadow

**Status:** Template — fill at Phase 5 exit before opening the Phase 6 entry PR.

| Field                       | Value                                |
| --------------------------- | ------------------------------------ |
| Authored by                 | _on-site engineer name_              |
| Bench Jetson box-id         | _e.g. bench-jetson-01_               |
...

## S-01 Exit Gates

| Gate                                                   | Bench week | Field 2 weeks | Evidence                                                 |
| ------------------------------------------------------ | ---------- | ------------- | -------------------------------------------------------- |
| #1 Zero daemon crashes ...                             | ☐ / ☐      | ☐ / ☐         | `journalctl ...` |
...

## Phase 6 entry approval

All four must hold for Phase 6 entry:

- ☐ All S-01 gates green over both soak windows (table above)
- ☐ R-02 replay-harness agreement rate ≥ 0.95 (artifact attached)
...

**Approved by:** _engineer signature / commit author_
**Date (ISO):** _YYYY-MM-DD_

---

*Phase 5: Bench & Field Shadow*
*Template authored by Plan 05-07; engineer-filled at Phase 5 exit.*
```

From .planning/phases/05.1-deb-packaging-hotfix/05.1-CONTEXT.md lines 162-173 (the 9 V-03 steps that become the 9 rows):
```
- **V-03:** **Bench Jetson EXIT bar** — operator-facing structured
  checklist committed at
  `.planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md`. Mirrors
  Phase 5's SIGNOFF.md template shape. Steps (each pass/fail):
  1. `.deb` built from the merged hotfix branch
  2. `scp` to bench Jetson, `dpkg -i` returns 0
  3. Operator provisions `/etc/spark-modem-watchdog/hmac-secret`
  4. `systemctl start spark-modem-watchdog.service` returns 0
  5. `systemctl is-active spark-modem-watchdog.service` reports `active`
  6. `journalctl -u spark-modem-watchdog.service` shows `Started ...` +
     no ERROR/CRITICAL lines
  7. `/run/spark-modem-watchdog/lock` present + owned by root
  8. `/run/spark-modem-watchdog/metrics.sock` present, scrape returns
     valid Prometheus text
  9. Daemon reaches Healthy on all 4 modems within 60s (NFR-13)
  Phase 05.1 EXIT = committed checklist with every box ticked.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author EXIT-CHECKLIST.md template</name>
  <files>.planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/05-bench-field-shadow/SIGNOFF.md (full file — the canonical shape)
    - .planning/phases/05.1-deb-packaging-hotfix/05.1-CONTEXT.md lines 162-173 (the 9 V-03 steps verbatim)
    - .planning/phases/05.1-deb-packaging-hotfix/05.1-PATTERNS.md § 14 (EXIT-CHECKLIST.md template with header table + 9-row gate table + approval footer + non-obvious conventions)
  </read_first>

  <action>
    # Implements V-03.

    Create the file `.planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md`
    with the EXACT content below. The file is operator-filled at phase exit;
    everything italicized with underscores (`_placeholder_`) is operator-
    fillable. Markdown table column-widths are NOT required to be aligned
    (SIGNOFF.md uses ASCII alignment as a courtesy, not a contract).

    File content:

    ```markdown
    # Phase 05.1 EXIT Checklist — deb-packaging-hotfix

    **Status:** Template — fill at Phase 05.1 exit before unblocking Plan 05-08 Task 2 (bench soak window).

    > **Forcing function:** Phase 05.1 EXIT bar (V-03 in
    > `.planning/phases/05.1-deb-packaging-hotfix/05.1-CONTEXT.md`):
    > bench Jetson runs the new `.deb`, daemon reaches `active (running)` with
    > `sd_notify READY=1`, every row below is PASS, this file is committed.

    | Field                       | Value                                |
    | --------------------------- | ------------------------------------ |
    | Authored by                 | _on-site engineer name_              |
    | Bench Jetson box-id         | _e.g. bench-jetson-01_               |
    | .deb commit SHA             | _short-sha (from merged hotfix branch)_ |
    | .deb build run URL          | _GitHub Actions URL_                 |
    | Install timestamp (ISO)     | _YYYY-MM-DDTHH:MM:SSZ_               |
    | EXIT timestamp (ISO)        | _YYYY-MM-DDTHH:MM:SSZ_               |

    ---

    ## V-03 Exit Gates (operator-filled)

    Every row must be PASS for Phase 05.1 EXIT. Mark each Status cell ☐ PASS
    or ☐ FAIL; fill Observed with the literal command output (truncate to a
    representative excerpt if multi-line — see Notes).

    | # | Step | Status | Command | Expected | Observed | Notes |
    |---|------|--------|---------|----------|----------|-------|
    | 1 | `.deb` built from merged hotfix branch | ☐ PASS / ☐ FAIL | `gh run view <run-id>` | green build; artifact `spark-modem-watchdog-arm64-deb` uploaded | _filled_ | |
    | 2 | scp + `dpkg -i` returns 0 | ☐ PASS / ☐ FAIL | `scp spark-modem-watchdog_*_arm64.deb bench-jetson:/tmp/ && ssh bench-jetson 'sudo dpkg -i /tmp/spark-modem-watchdog_*_arm64.deb'` | dpkg exits 0; postinst smoke green; placeholder HMAC secret written at /etc/spark-modem-watchdog/hmac-secret (0600 root:root) | _filled_ | |
    | 3 | Operator provisions `/etc/spark-modem-watchdog/hmac-secret` | ☐ PASS / ☐ FAIL | `head -c 32 /dev/urandom \| base64 \| sudo install -m 0600 -o root -g root /dev/stdin /etc/spark-modem-watchdog/hmac-secret` | file overwritten; `sudo stat -c '%a %u:%g' /etc/spark-modem-watchdog/hmac-secret` returns `600 0:0` | _filled_ | non-automatable (L-03) |
    | 4 | `systemctl start spark-modem-watchdog.service` returns 0 | ☐ PASS / ☐ FAIL | `sudo systemctl start spark-modem-watchdog.service && echo OK` | exit 0; ExecStartPre smoke + config-check both green; ExecStart fires | _filled_ | |
    | 5 | `systemctl is-active` reports `active` | ☐ PASS / ☐ FAIL | `systemctl is-active spark-modem-watchdog.service` | `active` | _filled_ | sd_notify READY=1 already fired |
    | 6 | `journalctl` shows `Started ...` + no ERROR/CRITICAL | ☐ PASS / ☐ FAIL | `journalctl -u spark-modem-watchdog.service --since='5 min ago' -p err` | empty / no ERROR or CRITICAL lines; `Started ...` line present in -p info | _filled_ | inspect for warnings about LoadCredential= on systemd 245 (L-04 may surface a journalctl warning here per CONTEXT.md L-04 third branch) |
    | 7 | `/run/spark-modem-watchdog/lock` present + owned by root | ☐ PASS / ☐ FAIL | `sudo stat -c '%n %a %u:%g' /run/spark-modem-watchdog/lock` | `/run/spark-modem-watchdog/lock 600 0:0` (or 0640; matches RuntimeDirectoryMode=0750) | _filled_ | |
    | 8 | `/run/spark-modem-watchdog/metrics.sock` scrape | ☐ PASS / ☐ FAIL | `sudo curl --unix-socket /run/spark-modem-watchdog/metrics.sock http://x/metrics \| head -20` | valid Prometheus text containing `modem_state_value`, `cycle_duration_seconds`, `actions_total` | _filled_ | |
    | 9 | Daemon reaches Healthy on all 4 modems within 60s (NFR-13) | ☐ PASS / ☐ FAIL | `sudo cat /var/lib/spark-modem-watchdog/status.json \| jq '.modems[] \| select(.state != "healthy") \| .modem'` after ≤60s | jq output is empty (every modem state == "healthy") | _filled_ | M5 readiness; verifies the end-to-end production path |

    ---

    ## Free-text rationale (≤500 words)

    > Engineer's narrative: what was observed, anything that needed a retry,
    > anything the next plan-phase should know. Especially: L-04 verdict
    > (silent-ignore vs hard-fail vs warning-with-degraded — see step 6
    > journalctl observation).

    _engineer fills here_

    ---

    ## Phase 05.1 EXIT approval

    All three must hold for Phase 05.1 EXIT:

    - ☐ Every row in the V-03 gate table above is PASS
    - ☐ The committed `.deb` matches the merged hotfix branch (commit SHA in header table)
    - ☐ Free-text rationale captures the L-04 verdict so future plans can act on it

    **Approved by:** _engineer signature / commit author_
    **Date (ISO):** _YYYY-MM-DD_

    ---

    *Phase 05.1: deb-packaging-hotfix*
    *Template authored by Plan 05.1-04; engineer-filled at Phase 05.1 exit.*
    ```

    Non-obvious conventions (PATTERNS.md § 14):
    - ☐ is Unicode U+2610 ("ballot box"); operator marks with ✅ or ❌
      at fill time (or simply ticks the box with `[x]` if rendering in
      GitHub markdown — engineer's call, both are recognized).
    - Underscore-italic `_placeholder_` for operator-fillable cells.
    - Section breaks: `---` on its own line.
    - Footer reference at file end matches SIGNOFF.md lines 122-124 shape.
    - `gh run view <run-id>` placeholder for step 1 — operator substitutes
      the real GitHub Actions run ID from the build-deb workflow URL.

    DO NOT pre-fill any operator field. The template is intentionally
    blank — committing a partially-filled template before all gates pass
    would falsely signal phase exit.
  </action>

  <verify>
    <automated>cd S:/spark/modem-watchdog &amp;&amp; test -f .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md &amp;&amp; echo OK</automated>
    <automated>cd S:/spark/modem-watchdog &amp;&amp; grep -c "^| [1-9] " .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md | grep -E '^9$' &gt; /dev/null &amp;&amp; echo OK</automated>
    <automated>cd S:/spark/modem-watchdog &amp;&amp; grep -c "☐ PASS / ☐ FAIL" .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md | awk '$1 &gt;= 9 {print "OK"}'</automated>
  </verify>

  <acceptance_criteria>
    - File `.planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md` exists and is non-empty.
    - `grep -F "V-03 Exit Gates" .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md` returns ≥1 line.
    - `grep -cE "^\| [1-9] " .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md` returns exactly 9 (9 gate rows).
    - `grep -cF "☐ PASS / ☐ FAIL" .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md` returns ≥9 (one per gate row).
    - `grep -F "Approved by:" .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md` returns 1 line.
    - `grep -F "Template authored by Plan 05.1-04" .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md` returns 1 line.
    - `grep -F "REPLACE_THIS_BEFORE_FIRST_START_SEE_RUNBOOK" .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md` returns 0 — the checklist does NOT contain the placeholder sentinel (operator provisions a REAL secret in step 3; the sentinel appears in postinst + config_check.py only, per Plan 02).
    - `grep -F "L-04" .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md` returns ≥1 line (operator is asked to capture the L-04 verdict in the free-text rationale).
    - `grep -F "NFR-13" .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md` returns ≥1 line (gate row 9 references the 60s NFR-13 bar).
  </acceptance_criteria>

  <done>
    EXIT-CHECKLIST.md is a complete operator-fillable template with header
    table, 9-row gate table, free-text section, approval footer, and footer
    reference. Mirrors SIGNOFF.md shape. Operator fills + commits at phase
    exit; the commit IS the phase EXIT signal.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → committed EXIT-CHECKLIST.md | the committed file is the phase-exit signal; a falsely-PASS-ticked checklist would prematurely unblock Plan 05-08 Task 2 (bench soak) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-14 | R (repudiation) | operator commits a partially-filled checklist; "every gate green" is unverified | accept | Single-operator workflow per CONTEXT.md specifics (the on-site engineer for Phase 5 Plan 05-08 is the same person). Header table includes `Approved by` + ISO date for git-blame attribution. Future phases can require a `git log -- EXIT-CHECKLIST.md` review at phase entry; not enforced by 05.1. |
| T-05.1-15 | I (info disclosure) | operator pastes the real HMAC secret bytes into the "Observed" column of step 3 | mitigate | Step 3 row Expected column explicitly asks for `stat -c '%a %u:%g'` output (mode + ownership only), NOT file contents. Notes column for step 3 explicitly flags it as "non-automatable (L-03)" so operator is reminded of the secret-discipline context. The instruction `head -c 32 /dev/urandom \| base64 \| install ...` pipes the secret directly to the file — secret bytes never appear on stdout/stderr. |
| T-05.1-16 | T (tampering) | EXIT-CHECKLIST.md template silently edited by a future plan to lower the bar | mitigate | The file lives in `.planning/phases/05.1-deb-packaging-hotfix/` — a CLOSED phase directory. Any edit to a closed-phase doc surfaces in PR review (closed phases are historical artifacts). Footer reference `*Template authored by Plan 05.1-04*` anchors provenance. |
</threat_model>

<verification>
After the task completes:

```bash
# File exists and has the structural elements.
test -f .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md
grep -F "V-03 Exit Gates" .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md
grep -cE "^\| [1-9] " .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md  # → 9
grep -F "Approved by:" .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md

# No accidental secrets pasted in (the placeholder sentinel does NOT belong
# in the operator checklist — only in postinst + config_check.py).
test "$(grep -c REPLACE_THIS_BEFORE_FIRST_START_SEE_RUNBOOK .planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md)" = "0"
```

The actual operator-fill-in cycle is OUT OF SCOPE for this plan and IS the
Phase 05.1 EXIT bar. This plan ships the empty template only.
</verification>

<success_criteria>
- `.planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md` exists with
  header table, 9 V-03 gate rows, free-text section, and approval footer.
- The template mirrors SIGNOFF.md shape.
- No operator field is pre-filled (the template is the contract — operator
  filling it is the phase-exit work).
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-deb-packaging-hotfix/05.1-04-SUMMARY.md`
following @$HOME/.claude/get-shit-done/templates/summary.md.

The SUMMARY must record:
- The created file path.
- The 9 gate-row titles (so Plan 05.1's parent orchestrator can confirm
  coverage matches CONTEXT.md L-162..173 verbatim).
- A note that the actual operator-filled fill-in is the Phase 05.1 EXIT bar
  and not in this plan's scope.
</output>
