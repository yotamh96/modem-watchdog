---
phase: 05.1-deb-packaging-hotfix
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - debian/changelog
autonomous: true
requirements_addressed: []
# Roadmap housekeeping + Debian metadata. No formal v1 REQ-ID maps.

must_haves:
  truths:
    - "ROADMAP.md Phase 05.1 entry no longer contains the literal string '[Urgent work - to be planned]'"
    - "ROADMAP.md Phase 05.1 entry has a real Goal line synthesized from CONTEXT.md (bench Jetson active + sd_notify READY=1 + 3 bugs fixed + regression gate)"
    - "ROADMAP.md Phase 05.1 entry has Plans: 6 plans (matches what /gsd-plan-phase 05.1 produced)"
    - "ROADMAP.md Phase 05.1 entry has a Plan list with 6 unchecked entries pointing at the 05.1-NN-PLAN.md files in this directory"
    - "debian/changelog has a NEW top entry with version 2.0.1-1 (PEP-440 patch bump), urgency=medium, UNRELEASED distribution"
    - "debian/changelog new entry describes the 3 fixes + regression gate in bullets"
    - "debian/changelog OLD 2.0.0-1 entry preserved verbatim below the new entry"
  artifacts:
    - path: .planning/ROADMAP.md
      provides: "Updated Phase 05.1 roadmap entry"
      contains: "spark-modem-watchdog"
    - path: debian/changelog
      provides: "Phase 05.1 hotfix changelog entry"
      contains: "Phase 05.1 hotfix"
  key_links:
    - from: ".planning/ROADMAP.md Phase 05.1 entry"
      to: "All 6 plan files in .planning/phases/05.1-deb-packaging-hotfix/"
      via: "Plans: list cross-references"
      pattern: "05.1-[0-9][0-9]-PLAN.md"
    - from: "debian/changelog version 2.0.1-1"
      to: "the 3 bug fixes + regression gate"
      via: "bulleted summary of what changed"
      pattern: "Phase 05.1 hotfix"

user_setup: []
---

<objective>
Land the two documentation/metadata changes that finalize the phase plan:

1. Rewrite ROADMAP.md Phase 05.1's placeholder Goal line (currently
   `Goal: [Urgent work - to be planned]`) and populate the Plans list to
   reflect the 6 plans `/gsd-plan-phase 05.1` produced. CONTEXT.md
   "Roadmap housekeeping" deferred-ideas section explicitly defers this to
   plan-phase, not discuss-phase.

2. Add a `2.0.1-1` entry to `debian/changelog` per CONTEXT.md "Claude's
   Discretion" (PEP-440 patch bump chosen over `2.0.0+hotfix.1` for
   simplicity and broad tooling compatibility — both are valid Debian
   version strings; 2.0.1 is the cleaner shape).

Purpose: future readers of ROADMAP.md (Phase 6 planner, future operators)
need to see what Phase 05.1 actually delivered. Future `apt-cache policy`
consumers need to see the version reflects the hotfix. Without this plan,
the Phase 05.1 entry stays as `[Urgent work - to be planned]` and the .deb
ships as `2.0.0-1` indistinguishable from the pre-hotfix package.

Output:
- `.planning/ROADMAP.md` Phase 05.1 entry rewritten.
- `debian/changelog` with a new top entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-deb-packaging-hotfix/05.1-CONTEXT.md
@.planning/phases/05.1-deb-packaging-hotfix/05.1-PATTERNS.md

<interfaces>
<!-- The current shape of the two files. -->

From .planning/ROADMAP.md lines 402-415 (the current Phase 05.1 entry — placeholder Goal preserved from /gsd-discuss-phase; Plans count + list ALREADY UPDATED by /gsd-plan-phase 05.1's update_roadmap step):
```markdown
### Phase 05.1: deb-packaging-hotfix (INSERTED)

**Goal:** [Urgent work - to be planned]
**Requirements**: TBD
**Depends on:** Phase 5
**Plans:** 6 plans

Plans:
- [ ] 05.1-01-PLAN.md — pyproject [project.scripts] + daemon _sync_main + debian/rules uv pip install . (I-01/I-02/I-04/I-05)
- [ ] 05.1-02-PLAN.md — Settings.resolve_hmac_secret_path() + ctl config-check verb + postinst HMAC placeholder (L-02/L-03/L-05)
- [ ] 05.1-03-PLAN.md — service unit ExecStart* paths repointed to /opt/.../python/bin/ (I-03; L-01 preserved)
- [ ] 05.1-04-PLAN.md — EXIT-CHECKLIST.md operator template (V-03)
- [ ] 05.1-05-PLAN.md — postinst smoke + unit-file audit + CI install test incl. systemd-analyze verify (V-01/V-02/V-04; L-04 verifier)
- [ ] 05.1-06-PLAN.md — ROADMAP.md goal rewrite + debian/changelog 2.0.1-1 entry
```

From debian/changelog (current full file — 10 lines):
```
spark-modem-watchdog (2.0.0-1) UNRELEASED; urgency=medium

  * Initial 2.0 release: Python rewrite of v1 bash toolchain.
  * Bundles CPython 3.12 via python-build-standalone (closes Q8).
  * Includes wire/ types, state_store/, subproc/, clock/, config/,
    event_logger/, and the default carrier table for IL/US/UK/DE.
  * No daemon yet — Phase 1 is plumbing only; the cycle/policy/actions
    land in Phase 2.

 -- spark-modem-watchdog devs <dev@draco.co.il>  Wed, 06 May 2026 00:00:00 +0000
```

From .planning/phases/05.1-deb-packaging-hotfix/05.1-PATTERNS.md § 10 (changelog format conventions verbatim):
- Two spaces between `--` and author; two spaces between author and date.
- Date format: `Tue, 12 May 2026 00:00:00 +0000` (RFC 822-ish; dpkg-parsechangelog strict).
- Indent of bullet lines: TWO spaces, asterisk, ONE space, text. Wraps indent by 4 spaces.
- `UNRELEASED` distribution preserved.
- `urgency=medium` matches the existing entry.

From .planning/ROADMAP.md lines 102-110 (the ROADMAP "Plans" shape that Phase 1 uses — the planner copies this structure):
```markdown
**Plans**: 7 plans

Plans:
- [x] 01-01-PLAN.md — Repo + lint/CI scaffolding (pyproject.toml, requirements.lock with 10 pinned runtime libs...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite ROADMAP.md Phase 05.1 entry</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 402-415 — the Phase 05.1 entry to rewrite; Plans count + list are already populated by the planner, only Goal + Success Criteria remain. Also see lines 102-110 for the Phase 1 Plans-list shape pattern)
    - .planning/phases/05.1-deb-packaging-hotfix/05.1-CONTEXT.md "Phase Boundary" section (lines 8-44 — source of truth for the Goal line)
    - .planning/phases/05.1-deb-packaging-hotfix/05.1-CONTEXT.md "Deferred Ideas" § "Roadmap housekeeping" (lines 405-408 — explicit deferral to plan-phase)
  </read_first>

  <action>
    # Implements CONTEXT.md "Deferred Ideas" § "Roadmap housekeeping".

    Edit `.planning/ROADMAP.md`. Replace the entire current Phase 05.1 entry
    block (the section starting with `### Phase 05.1: deb-packaging-hotfix
    (INSERTED)` through the last `- [ ] 05.1-06-PLAN.md ...` line — currently
    spans ~lines 402-415; the Plans count + list were ALREADY rewritten by
    the planner's update_roadmap step, so the only remaining work is the
    Goal-line rewrite + the Success Criteria block addition) with the
    following block:

    ```markdown
    ### Phase 05.1: deb-packaging-hotfix (INSERTED)

    **Goal**: `dpkg -i spark-modem-watchdog_2.0.*_arm64.deb` followed by
    `systemctl start spark-modem-watchdog.service` reaches `active (running)`
    with `sd_notify READY=1` on a bench Jetson (JetPack 5.1.5 / Ubuntu 20.04 /
    systemd 245 / aarch64). Three known bugs fixed: (1) `spark_modem` not on
    `sys.path` of the bundled venv → fixed by `uv pip install .` inside
    `override_dh_auto_install`; (2) no daemon entry point → fixed by adding
    `spark-modem-watchdog` to `pyproject.toml [project.scripts]`; (3) systemd-
    245 `LoadCredential=` incompatibility → fixed by a code-side fallback in
    `Settings.resolve_hmac_secret_path()`. Regression gate (D-01) lands so
    the same class of bug cannot recur silently.

    **Requirements**: (no formal v1 REQ-IDs — inserted hotfix; indirectly
    tied to NFR-30 root-only secrets via L-03 mode/owner check, and ADR-0011
    HMAC discipline via L-01..L-05)

    **Depends on**: Phase 5

    **Success Criteria** (EXIT bar pattern per D-03, mirroring Phase 4):
      The committed `.planning/phases/05.1-deb-packaging-hotfix/EXIT-CHECKLIST.md`
      has every V-03 gate row marked PASS by the on-site engineer. The
      checklist's 9 rows are: (1) .deb built from merged hotfix branch;
      (2) `dpkg -i` returns 0; (3) operator provisions HMAC secret;
      (4) `systemctl start` returns 0; (5) `is-active` reports `active`;
      (6) `journalctl` shows `Started ...` with no ERROR/CRITICAL;
      (7) `/run/spark-modem-watchdog/lock` present + owned by root;
      (8) `/run/spark-modem-watchdog/metrics.sock` scrape returns valid
      Prometheus text; (9) daemon reaches Healthy on all 4 modems within
      60s (NFR-13). The aarch64 GHA install test (V-02) gates the .deb
      artifact in CI; the cross-platform unit-file audit (V-04) gates the
      unit ↔ pyproject ↔ install layout on every dev-host pytest.

    **Plans**: 6 plans

    Plans:
    - [ ] 05.1-01-PLAN.md — pyproject.toml [project.scripts] + daemon _sync_main inline + debian/rules uv pip install . + .install/.dirs lib-line removal (I-01, I-02, I-04, I-05)
    - [ ] 05.1-02-PLAN.md — Settings.resolve_hmac_secret_path() (L-02) + ctl config-check verb (L-05) + postinst HMAC placeholder write (L-03)
    - [ ] 05.1-03-PLAN.md — debian/spark-modem-watchdog.service ExecStart* paths repointed to /opt/.../python/bin/ (I-03; L-01 preserved)
    - [ ] 05.1-04-PLAN.md — EXIT-CHECKLIST.md operator template (V-03)
    - [ ] 05.1-05-PLAN.md — Postinst smoke extension (V-01) + unit-file audit V-04 (a/b/c) + CI install test strict superset incl. systemd-analyze verify (V-02 + L-04 forcing function)
    - [ ] 05.1-06-PLAN.md — ROADMAP.md goal rewrite + debian/changelog 2.0.1-1 entry
    ```

    Where the entry lands in the file: the Phase 05.1 block stays in its
    current location between Phase 5 (ends at line ~400) and Phase 6
    (starts at line ~412). The line count of the new block is ~30 lines
    (vs. ~9 in the old block) — Phase 6's section heading shifts down
    accordingly but its content is byte-for-byte unchanged.

    Do NOT touch:
    - The roadmap "Overview" section (lines 1-29).
    - Phase 1..Phase 5 entries (lines 38-400).
    - Phase 6 / Phase 7 entries (lines 412-end).
    - The "Progress" table at the end of the file (lines ~485+).

    The Progress table currently shows Phase 05.1 implicitly within the
    "98%" stopped_at marker; do NOT add a Phase 05.1 row to that table
    (Phase 05.1 is an inserted decimal phase per the table's framing at
    lines 32-36).
  </action>

  <verify>
    <automated>cd S:/spark/modem-watchdog &amp;&amp; grep -c "\[Urgent work - to be planned\]" .planning/ROADMAP.md | grep -E '^0$' &gt; /dev/null &amp;&amp; echo OK</automated>
    <automated>cd S:/spark/modem-watchdog &amp;&amp; grep -c "Phase 05.1: deb-packaging-hotfix (INSERTED)" .planning/ROADMAP.md | grep -E '^1$' &gt; /dev/null &amp;&amp; echo OK</automated>
    <automated>cd S:/spark/modem-watchdog &amp;&amp; grep -c "05.1-0[1-6]-PLAN.md" .planning/ROADMAP.md | awk '$1 &gt;= 6 {print "OK"}'</automated>
    <automated>cd S:/spark/modem-watchdog &amp;&amp; grep -F "**Plans**: 6 plans" .planning/ROADMAP.md &gt; /dev/null &amp;&amp; echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -F "[Urgent work - to be planned]" .planning/ROADMAP.md` returns 0 matches (placeholder removed).
    - `grep -F "Phase 05.1: deb-packaging-hotfix (INSERTED)" .planning/ROADMAP.md` returns 1 line.
    - `grep -F "sd_notify READY=1" .planning/ROADMAP.md` returns ≥1 line (concrete goal language).
    - `grep -cF "05.1-0" .planning/ROADMAP.md` returns ≥6 (one per Plans-list entry).
    - `grep -F "05.1-01-PLAN.md" .planning/ROADMAP.md` returns ≥1 line.
    - `grep -F "05.1-02-PLAN.md" .planning/ROADMAP.md` returns ≥1 line.
    - `grep -F "05.1-03-PLAN.md" .planning/ROADMAP.md` returns ≥1 line.
    - `grep -F "05.1-04-PLAN.md" .planning/ROADMAP.md` returns ≥1 line.
    - `grep -F "05.1-05-PLAN.md" .planning/ROADMAP.md` returns ≥1 line.
    - `grep -F "05.1-06-PLAN.md" .planning/ROADMAP.md` returns ≥1 line.
    - `grep -F "**Plans**: 6 plans" .planning/ROADMAP.md` returns 1 line.
    - `grep -F "**Goal**:" .planning/ROADMAP.md` count is unchanged (Phase 1..7 each have one) — verifiable: count should equal the number of Phase entries in the file. After this edit, the Phase 05.1 line in the file uses `**Goal**:` matching the convention.
    - Phase 1 entry block (`### Phase 1: Foundations & ADRs` section) is unchanged: `grep -F "Phase 1: Foundations & ADRs" .planning/ROADMAP.md` returns 1 line, and the line immediately after still contains the "Goal" line for Phase 1.
    - Phase 6 entry block (`### Phase 6: Cutover & Fleet Rollout`) is unchanged: `grep -F "Phase 6: Cutover & Fleet Rollout" .planning/ROADMAP.md` returns 1 line.
  </acceptance_criteria>

  <done>
    Phase 05.1 entry has a real Goal line synthesized from CONTEXT.md, a
    Success Criteria block describing the EXIT bar, a list of 6 plan
    cross-references, and no `[Urgent work - to be planned]` placeholder.
    Phase 1..5 + Phase 6..7 entries are byte-for-byte unchanged.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 2.0.1-1 entry to debian/changelog</name>
  <files>debian/changelog</files>
  <read_first>
    - debian/changelog (full 10-line current file)
    - .planning/phases/05.1-deb-packaging-hotfix/05.1-PATTERNS.md § 10 (changelog format conventions verbatim)
    - .planning/phases/05.1-deb-packaging-hotfix/05.1-CONTEXT.md "Claude's Discretion" lines 192-196 (version bump shape — Plan picks 2.0.1-1)
  </read_first>

  <action>
    # Implements CONTEXT.md "Claude's Discretion" version bump.

    Edit `debian/changelog`. Prepend a NEW entry above the existing 2.0.0-1
    entry. The existing entry stays byte-for-byte unchanged below the new
    one. Final file:

    ```
    spark-modem-watchdog (2.0.1-1) UNRELEASED; urgency=medium

      * Phase 05.1 hotfix: bench Jetson .deb install now reaches active (running)
        with sd_notify READY=1.
      * Fix (bug #1): ship spark_modem package via `uv pip install .` inside
        override_dh_auto_install (I-01); removes the
        src/spark_modem /opt/spark-modem-watchdog/lib/ install map line that
        left the daemon package off the bundled venv's sys.path.
      * Fix (bug #2): add `spark-modem-watchdog` console script (I-02 + I-04) —
        systemd ExecStart no longer hits 203/EXEC. Daemon entry point is now
        /opt/spark-modem-watchdog/python/bin/spark-modem-watchdog.
      * Fix (bug #3): HMAC-secret fallback for systemd 245 (L-02) — daemon
        reads from /etc/spark-modem-watchdog/hmac-secret directly if
        CREDENTIALS_DIRECTORY is unset (Ubuntu 20.04 ships systemd 245;
        LoadCredential= was introduced in systemd 247). LoadCredential=
        directive preserved in the unit file (L-01) for future systemd 247+
        boxes.
      * Postinst now writes a 0600 root:root placeholder HMAC secret (L-03);
        `spark-modem ctl config-check` (ExecStartPre) refuses to boot with
        the placeholder sentinel.
      * Regression gate: extended postinst smoke now imports
        spark_modem.daemon.main + spark_modem.cli.main (V-01); new aarch64
        Docker install test in CI asserts both console scripts present,
        HMAC placeholder file 0600 root:root, and systemd-analyze verify
        exits 0 (V-02; verifies L-04); 3 new unit-file audit assertions
        catch unit ↔ pyproject ↔ install drift (V-04).

     -- spark-modem-watchdog devs <dev@draco.co.il>  Tue, 12 May 2026 00:00:00 +0000

    spark-modem-watchdog (2.0.0-1) UNRELEASED; urgency=medium

      * Initial 2.0 release: Python rewrite of v1 bash toolchain.
      * Bundles CPython 3.12 via python-build-standalone (closes Q8).
      * Includes wire/ types, state_store/, subproc/, clock/, config/,
        event_logger/, and the default carrier table for IL/US/UK/DE.
      * No daemon yet — Phase 1 is plumbing only; the cycle/policy/actions
        land in Phase 2.

     -- spark-modem-watchdog devs <dev@draco.co.il>  Wed, 06 May 2026 00:00:00 +0000
    ```

    PATTERNS.md § 10 non-obvious conventions (verify after edit):
    - Two spaces between ` -- ` and author. The author line literally starts
      with `<space><space>-- <space>` then author, then `<space><space>`
      then date.
    - Date format: `Tue, 12 May 2026 00:00:00 +0000` (RFC 822-ish).
    - Indent of bullet lines: TWO spaces, asterisk, ONE space, text.
    - Wrap-continuation indent: 4 spaces.
    - `UNRELEASED` distribution preserved (changes to a real distro only at
      release-build time).
    - `urgency=medium` matches the existing entry; hotfixes could justify
      `high`, but `medium` is safer (some tooling treats `high` as
      alarm-worthy per PATTERNS.md § 10).

    Version 2.0.1-1 chosen (PEP-440 patch) over 2.0.0+hotfix.1 (PEP-440
    local-version) per CONTEXT.md "Claude's Discretion" line 193. Reasons:
    (a) cleaner shape, (b) every PEP-440 + Debian-version tool understands
    `M.m.p` without quirks, (c) `+` in version strings is occasionally
    handled inconsistently by older apt versions (NFR-50 fleet has Ubuntu
    20.04 boxes).

    DO NOT touch any other line. DO NOT change the existing 2.0.0-1 entry.
    DO NOT change the date format. DO NOT change the urgency.

    Date: use literal `Tue, 12 May 2026 00:00:00 +0000` (matches today's
    date per the project's `currentDate` context: 2026-05-12).
  </action>

  <verify>
    <automated>cd S:/spark/modem-watchdog &amp;&amp; head -1 debian/changelog | grep -F "spark-modem-watchdog (2.0.1-1) UNRELEASED" &gt; /dev/null &amp;&amp; echo OK</automated>
    <automated>cd S:/spark/modem-watchdog &amp;&amp; grep -c "^spark-modem-watchdog (" debian/changelog | grep -E '^2$' &gt; /dev/null &amp;&amp; echo OK</automated>
    <automated>cd S:/spark/modem-watchdog &amp;&amp; grep -F "Phase 05.1 hotfix" debian/changelog &gt; /dev/null &amp;&amp; echo OK</automated>
    <automated>cd S:/spark/modem-watchdog &amp;&amp; which dpkg-parsechangelog 2&gt;/dev/null &amp;&amp; dpkg-parsechangelog -l debian/changelog | head -5 || echo "(dpkg-parsechangelog not on dev host — V-02 CI install test exercises it)"</automated>
  </verify>

  <acceptance_criteria>
    - `head -1 debian/changelog` is exactly `spark-modem-watchdog (2.0.1-1) UNRELEASED; urgency=medium`.
    - `grep -c "^spark-modem-watchdog (" debian/changelog` returns 2 (the new 2.0.1-1 entry + the preserved 2.0.0-1 entry).
    - `grep -c "^spark-modem-watchdog (2.0.0-1) UNRELEASED" debian/changelog` returns 1 (old entry preserved verbatim).
    - `grep -F "Phase 05.1 hotfix" debian/changelog` returns ≥1 line.
    - `grep -F "203/EXEC" debian/changelog` returns ≥1 line.
    - `grep -F "I-01" debian/changelog` returns ≥1 line.
    - `grep -F "L-02" debian/changelog` returns ≥1 line.
    - `grep -F "V-02" debian/changelog` returns ≥1 line.
    - `grep -cE "^ --" debian/changelog` returns 2 (one author line per entry).
    - `grep -cE "^ -- spark-modem-watchdog devs &lt;dev@draco.co.il&gt;" debian/changelog` returns 2.
    - The new entry's date is `Tue, 12 May 2026 00:00:00 +0000`.
    - If `dpkg-parsechangelog` is available (Linux dev host or CI): `dpkg-parsechangelog -l debian/changelog --count 1` exits 0 and prints Version: 2.0.1-1. On Windows dev host, this is skipped — V-02 CI install test exercises it.
    - The file ends with a newline (POSIX).
  </acceptance_criteria>

  <done>
    `debian/changelog` has a new top entry at version 2.0.1-1 describing the
    3 bug fixes + regression gate; the existing 2.0.0-1 entry is preserved
    verbatim below; date format follows the project's existing convention.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed ROADMAP.md → future planner / operator | the roadmap is documentation; misleading content propagates as misunderstanding |
| committed debian/changelog → built .deb metadata | `dpkg -i` consumers see the version + bullets; downstream apt-cache policy + apt-list-changes consumers see them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-24 | I (info disclosure) | the new changelog bullets leak internal decision-tag identifiers (I-01, L-02, V-02, etc.) into a shipped .deb that customers might unpack | accept | The decision tags are non-sensitive engineering metadata. Their value to a future operator debugging an install ("oh, I-02 is what added the daemon entry point") outweighs the negligible exposure to a customer who runs `dpkg -e <deb>` (a deliberate inspection action). |
| T-05.1-25 | R (repudiation) | committed ROADMAP says "6 plans" but a future plan-phase silently adds a 7th plan | mitigate | The "Plans: 6 plans" line is a roadmap commitment; a future plan-phase that adds a 7th plan MUST update the count via /gsd-plan-phase tooling (which is hands-off-this-plan to fix at that point). Plan 05.1-05's V-04 audit does NOT cover this drift — it's a ROADMAP-shape concern, not a code-shape concern. Acceptable: roadmap drift surfaces as a PR review concern, not a CI-blocking one. |
| T-05.1-26 | T (tampering) | debian/changelog format drift breaks dpkg-parsechangelog | mitigate | PATTERNS.md § 10 non-obvious conventions are followed (TWO spaces before author, exact date format, exact `UNRELEASED` distribution, exact `urgency=medium`). Plan 05.1-05's V-02 CI install test runs `apt-get install /dist/<deb>` which exercises dpkg-parsechangelog (apt cannot install a deb with a malformed changelog) — if format drifts, CI fails. |
| T-05.1-27 | D (DoS / availability) | version 2.0.1-1 collides with an existing apt-pinned version on a Phase 6 cutover box | accept | apt-pin semantics: a higher version always wins on `dpkg -i`. 2.0.1 > 2.0.0; no rollback complication for `dpkg -i` itself. Rollback path (per MIGRATION.md §5) is `apt purge` then reinstall v1 .deb, not version-pinning. No mitigation needed. |
</threat_model>

<verification>
After both tasks complete:

```bash
# ROADMAP placeholder gone.
grep -c "\[Urgent work - to be planned\]" .planning/ROADMAP.md   # → 0

# ROADMAP has the 6 plan cross-refs.
for i in 01 02 03 04 05 06; do
  grep -F "05.1-${i}-PLAN.md" .planning/ROADMAP.md || echo "MISSING: 05.1-${i}-PLAN.md"
done

# changelog has both entries.
grep -c "^spark-modem-watchdog (" debian/changelog   # → 2

# changelog top entry is the new one.
head -1 debian/changelog | grep -F "(2.0.1-1)"

# If on Linux + dpkg-parsechangelog installed: top entry parses.
which dpkg-parsechangelog >/dev/null && dpkg-parsechangelog -l debian/changelog --count 1
```
</verification>

<success_criteria>
- ROADMAP.md Phase 05.1 entry has a real Goal + 6 plan cross-refs + a real
  Success Criteria block (EXIT bar pattern).
- debian/changelog has a new 2.0.1-1 entry above the preserved 2.0.0-1
  entry; both follow the PATTERNS.md § 10 format conventions.
- No other ROADMAP entry (Phase 1..7) is modified.
- No other changelog entry is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-deb-packaging-hotfix/05.1-06-SUMMARY.md`
following @$HOME/.claude/get-shit-done/templates/summary.md.

The SUMMARY must record:
- The before/after Phase 05.1 ROADMAP entry (full text — it's small).
- The new debian/changelog top entry verbatim.
- Confirmation that Phase 1..5 + Phase 6..7 ROADMAP entries are
  byte-for-byte unchanged (e.g. via a `diff` against the pre-edit
  ROADMAP.md if available, or via cross-reference of section line counts).
</output>
