# Phase 05.1: deb-packaging-hotfix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-11
**Phase:** 05.1-deb-packaging-hotfix
**Areas discussed:** Hotfix scope discipline, Install + entry-point + bin/ layout, LoadCredential fallback (systemd 245), Validation gate

---

## Hotfix scope discipline

| Option | Description | Selected |
|--------|-------------|----------|
| Minimum + regression gate | Fix 3 bugs + extend smoke + add CI install test; retire bug class | ✓ |
| Strict minimum | Fix 3 bugs only, smallest possible diff | |
| Light .deb pipeline review | 3 bugs + regression gate + broader review for other latent .deb bugs | |

**User's choice:** Minimum + regression gate

| Option | Description | Selected |
|--------|-------------|----------|
| pyproject only | Add `[project.scripts]` entry; no lockfile change | ✓ |
| pyproject + lockfile refresh | Refresh requirements.lock as well | |
| Neither — wrapper-only fix | Bash wrapper, no pyproject edits | |

**User's choice:** pyproject only

| Option | Description | Selected |
|--------|-------------|----------|
| Exit gate = bench Jetson green | Mirror Phase 4's "first nightly HIL" exit-bar idiom | ✓ |
| Full ROADMAP SC block | Author SC #1..#N in ROADMAP.md | |
| Merge into Phase 5 retroactively | Cancel 05.1; amend Phase 5 | |

**User's choice:** Exit gate = bench Jetson green

| Option | Description | Selected |
|--------|-------------|----------|
| Glue + tiny daemon-side hook | Mostly debian/* + scripts/* + pyproject; one src/ change for HMAC fallback | ✓ |
| Glue only | Zero src/ changes; all-systemd HMAC workaround | |
| Open for broader src/ work | Any src/ change the fix needs | |

**User's choice:** Glue + tiny daemon-side hook
**Notes:** Daemon-side change limited to the HMAC fallback path in `config/settings.py`.

---

## Install + entry-point + bin/ layout

| Option | Description | Selected |
|--------|-------------|----------|
| uv pip install . --no-deps | Install spark_modem into bundled venv as proper package; console_scripts auto-generate | ✓ |
| .pth file pointing to /opt/.../lib/ | Drop a .pth in venv site-packages | |
| Copy src into site-packages directly | Change debian/install destination | |

**User's choice:** uv pip install . --no-deps

| Option | Description | Selected |
|--------|-------------|----------|
| pyproject [project.scripts] | Add `spark-modem-watchdog = "spark_modem.daemon.main:_sync_main"` | ✓ |
| ExecStart `python -m spark_modem.daemon` | Skip console_script; invoke module directly | |
| Hand-written bash wrapper | scripts/spark-modem-watchdog wrapper | |

**User's choice:** pyproject [project.scripts]

| Option | Description | Selected |
|--------|-------------|----------|
| Point systemd at venv bin/ directly | Drop /opt/.../bin/; systemd ExecStart references python/bin/ | ✓ |
| Symlink farm at /opt/.../bin/ | Postinst creates symlinks; preserves "public" bin path | |
| /opt/.../bin/ holds bash wrappers only | Only relevant if wrapper-based entry point picked | |

**User's choice:** Point systemd at venv bin/ directly

| Option | Description | Selected |
|--------|-------------|----------|
| Inline in spark_modem.daemon.main | _sync_main next to existing async def main() | ✓ |
| Separate entry module | src/spark_modem/daemon/__main__.py with asyncio.run wrapper | |
| ExecStart uses python -m, no shim needed | Skip the sync shim entirely | |

**User's choice:** Inline in spark_modem.daemon.main

---

## LoadCredential fallback (systemd 245)

| Option | Description | Selected |
|--------|-------------|----------|
| Keep LoadCredential + code fallback | Leave directive in unit; daemon falls back when CREDENTIALS_DIRECTORY unset | ✓ |
| Drop LoadCredential entirely | Remove directive; daemon always reads from /etc/.../hmac-secret | |
| Conditional unit via debian/postinst | Postinst detects systemd version; writes drop-in override | |

**User's choice:** Keep LoadCredential + code fallback

| Option | Description | Selected |
|--------|-------------|----------|
| Operator pre-start, placeholder rejected | Postinst writes 0600 placeholder; config-check rejects it | ✓ |
| Postinst auto-generates per box | Random 256-bit secret per box; defeats fleet-wide receiver setup | |
| Daemon refuses to start until provisioned | Postinst skips file entirely | |

**User's choice:** Operator pre-start, placeholder rejected

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — part of ExecStartPre | Extend config-check to verify exists/not-placeholder/mode 0600/owner root | ✓ |
| Only verify file exists | Faster, less invasive; placeholder check moves to daemon | |
| Skip — daemon handles it on first webhook | Decoupled but defers diagnostic | |

**User's choice:** Yes — part of ExecStartPre

| Option | Description | Selected |
|--------|-------------|----------|
| Verify experimentally in the plan | Spin Ubuntu 20.04 arm64 container, run systemd-analyze verify on the unit | ✓ |
| Assume silent-ignore, code-side fallback alone | Skip verification; trust silent-ignore behavior | |
| Assume hard-fail, conditional unit always | Conservative; add drop-in override regardless | |

**User's choice:** Verify experimentally in the plan

---

## Validation gate

| Option | Description | Selected |
|--------|-------------|----------|
| Runtime libs + spark_modem entry modules | Extend smoke to import spark_modem.daemon.main + spark_modem.cli.main | ✓ |
| Add deeper subtree import | Also import wire/, qmi/, policy/, state_store/ | |
| Add running-binary check | Also exec the console scripts as a smoke step | |

**User's choice:** Runtime libs + spark_modem entry modules

| Option | Description | Selected |
|--------|-------------|----------|
| Dockerized aarch64 Ubuntu 20.04 | New GitHub Actions job; container apt install; assertions on layout + systemd-analyze | ✓ |
| Local-arch ubuntu:20.04 only | Use qemu-user-static on default x86_64 runner | |
| Skip CI install test — bench Jetson is enough | Trust bench Jetson manual verification alone | |

**User's choice:** Dockerized aarch64 Ubuntu 20.04

| Option | Description | Selected |
|--------|-------------|----------|
| Manual + structured checklist | Author EXIT-CHECKLIST.md mirroring SIGNOFF.md pattern | ✓ |
| Reuse Phase 5 SIGNOFF.md | Add a "Phase 05.1 prerequisite" subsection to existing SIGNOFF | |
| Free-text bench verification in plan SUMMARY | Plan SUMMARY.md narrative; no structured file | |

**User's choice:** Manual + structured checklist

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — extend Plan 03-08's audit | Add 2-3 assertions to test_unit_file_audit.py | ✓ |
| Yes — author a new audit file | New tests/integration/test_deb_install.py | |
| Skip — CI install test covers it | Don't extend any audit; Dockerized test alone | |

**User's choice:** Yes — extend Plan 03-08's audit

---

## Claude's Discretion

The user explicitly deferred these decisions to planning:

- Exact Docker image tag for V-02 (ubuntu:20.04 vs arm64v8/ubuntu:20.04 vs pinned-by-digest).
- Changelog versioning bump shape (2.0.0 → 2.0.1 vs 2.0.0+hotfix.1).
- Exact placeholder sentinel string for the HMAC file.
- Whether L-04 verification lands in V-02 CI or as a one-shot research task (V-02 is the cleanest forcing function).
- Whether `spark-modem-watchdog` should grow a `--version` flag.
- Whether `_sync_main` propagates `KeyboardInterrupt` cleanly (inherits asyncio.run semantics — fine for systemd Type=notify).

## Deferred Ideas

Out of Phase 05.1 scope, recorded so they aren't lost:

- Broader `debian/rules` end-to-end review (dh_shlibdeps, postinst idempotency, dpkg --configure -a recovery).
- requirements.lock refresh (would require HIL nightly re-run before SIGNOFF; worth a separate cycle).
- systemd-247-on-Jetson upgrade as a future fleet prerequisite.
- `debian/control` Depends= review for any new system-package dependencies.
- `--version` flag on spark-modem-watchdog (Claude's Discretion if scope-clean).
- ROADMAP.md Phase 05.1 goal rewrite (happens during /gsd-plan-phase 05.1).
